package ru.javarush.java.core.level20.task02;

/*
Художник, Мастер Линий и Кругов

Вы работаете над созданием интерактивного приложения для рисования. В этом приложении каждая фигура должна знать, как себя отобразить на холсте. Вы хотите, чтобы все фигуры, независимо от их типа, можно было "попросить" нарисовать себя универсальным способом.
Сперва определите универсальный чертежный контракт под названием Drawable, который включает в себя одно действие: draw(), не возвращающее ничего. Затем создайте конкретную фигуру — Circle (Круг). Этот Circle должен чётко понимать, как выполнить контракт Drawable, и при вызове draw() он должен объявлять: "Drawing a circle".
Теперь в вашей основной программе, представьте, что у вас есть универсальный инструмент для рисования, который может удерживать любой объект, соответствующий контракту Drawable. Присвойте этому инструменту ваш только что созданный Circle. Затем прикажите вашему инструменту draw(). В результате на экране должно появиться сообщение "Drawing a circle".

Требования:
•	Должен быть объявлен интерфейс с именем Drawable.
•	Интерфейс Drawable должен содержать абстрактный метод draw(), который не принимает параметров и не возвращает значение (void).
•	Должен быть создан класс Circle, который реализует интерфейс Drawable.
•	В классе Circle должен быть реализован метод draw(), который выводит на экран строку "Drawing a circle".
•	В основной программе должна быть объявлена переменная типа Drawable, которой присваивается объект класса Circle.
•	Метод draw() должен быть вызван у переменной типа Drawable, и в результате на экран должно быть выведено сообщение "Drawing a circle".

public class Solution {
    public static void main(String[] args) {
        // Объявляем переменную универсального типа интерфейса Drawable — "инструмент для рисования"
        Drawable tool = new Circle(); // Присваиваем ей конкретную фигуру — круг

        // Вызываем метод draw() через переменную интерфейсного типа
        tool.draw(); // Ожидаемый вывод: "Drawing a circle"
    }
}

 */

public class Solution {
    public static void main(String[] args) {
        // Объявляем переменную универсального типа интерфейса Drawable — "инструмент для рисования"
        Drawable tool = new Circle(); // Присваиваем ей конкретную фигуру — круг

        // Вызываем метод draw() через переменную интерфейсного типа
        tool.draw(); // Ожидаемый вывод: "Drawing a circle"
    }
}

class Circle implements Drawable {
    @Override
    public void draw() {
        System.out.println("Drawing a circle");
    }
}



/*

// Универсальный "чертежный контракт"
interface Drawable {
    void draw(); // Действие "нарисовать", не принимает параметров и ничего не возвращает
}

// Конкретная фигура: Круг
class Circle implements Drawable {
    @Override
    public void draw() {
        // Реализация контракта: при рисовании круга выводим сообщение
        System.out.println("Drawing a circle");
    }
}
 */
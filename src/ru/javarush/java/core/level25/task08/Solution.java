package ru.javarush.java.core.level25.task08;

/*
Цифровой Холст: Расширенные Возможности Рисования 🆕

Вы – художник-программист, и ваша задача – создать основу для графического редактора, который работает с различными отрисовываемыми объектами.
Определите интерфейс Renderable с основным методом paint(), который будет отвечать за саму отрисовку.
Однако, современные инструменты требуют больше гибкости! Добавьте в ваш интерфейс Renderable новый default-метод adjustScale(), который позволит изменять размер объекта по умолчанию, например, выводя сообщение "Размер объекта скорректирован по умолчанию.".
А для универсальных операций, не зависящих от конкретного объекта, таких как проверка цветовой палитры, добавьте static-метод validateColorPalette(), который мог бы вывести "Цветовая палитра проверена, всё в порядке!".
В главном методе main создайте простую реализацию Renderable (например, анонимный класс или простой класс Shape), вызовите метод paint(), затем используйте adjustScale() на вашем объекте, и, наконец, вызовите статический validateColorPalette() напрямую из интерфейса, чтобы продемонстрировать все новые возможности.

Требования:
•	Необходимо создать интерфейс с именем Renderable.
•	Интерфейс Renderable должен содержать абстрактный метод paint() без параметров.
•	В интерфейсе Renderable должен быть реализован default-метод adjustScale(), который по умолчанию выводит строку "Размер объекта скорректирован по умолчанию.".
•	В интерфейсе Renderable должен быть реализован static-метод validateColorPalette(), который выводит строку "Цветовая палитра проверена, всё в порядке!".
•	В методе main необходимо создать реализацию интерфейса Renderable (например, с помощью отдельного класса или анонимного класса).
•	В методе main на объекте, реализующем Renderable, должен быть вызван метод paint().
•	В методе main на объекте, реализующем Renderable, должен быть вызван метод adjustScale().
•	В методе main должен быть вызван статический метод validateColorPalette() напрямую через Renderable.

public class Solution {
    public static void main(String[] args) {
        // Простейшая реализация Renderable через анонимный класс


        // Вызываем обязательный метод отрисовки


        // Демонстрация default-метода интерфейса


        // Вызов статического метода напрямую через интерфейс

    }
}

 */

public class Solution {
    public static void main(String[] args) {
        // Простейшая реализация Renderable через анонимный класс
        Renderable myPic = new Renderable() {
            @Override
            public void paint() {
                System.out.println("Я рисую свою картину!");
            }

        };

        // Вызываем обязательный метод отрисовки
        myPic.paint();

        // Демонстрация default-метода интерфейса
        myPic.adjustScale();

        // Вызов статического метода напрямую через интерфейс
        Renderable.validateColorPalette();

    }
}

interface Renderable {
    void paint();

    static void validateColorPalette() {
        System.out.println("Цветовая палитра проверена, всё в порядке!");
    }
    default void adjustScale() {
        System.out.println("Размер объекта скорректирован по умолчанию.");
    }
}



/*
// Интерфейс с обязательным методом paint(), default-методом и static-методом
interface Renderable {
    // Абстрактный метод: каждая реализация отвечает за свою отрисовку
    void paint();

    // Default-метод: поведение по умолчанию, доступно всем реализациям
    default void adjustScale() {
        System.out.println("Размер объекта скорректирован по умолчанию.");
    }

    // Статический метод интерфейса: общая утилита, вызывается через имя интерфейса
    static void validateColorPalette() {
        System.out.println("Цветовая палитра проверена, всё в порядке!");
    }
}

public class Solution {
    public static void main(String[] args) {
        // Простейшая реализация Renderable через анонимный класс
        Renderable shape = new Renderable() {
            @Override
            public void paint() {
                // Имитируем отрисовку объекта
                System.out.println("Отрисовка объекта: простой квадрат.");
            }
        };

        // Вызываем обязательный метод отрисовки
        shape.paint();

        // Демонстрация default-метода интерфейса
        shape.adjustScale();

        // Вызов статического метода напрямую через интерфейс
        Renderable.validateColorPalette();
    }
}
 */
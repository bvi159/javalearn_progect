package ru.javarush.java.core.level25.task09;

/*
Запуск Сложнейшей Системы: Порядок Инициализации 🔄

Представьте, что вы запускаете очень сложную программную систему SystemCore, которая требует тщательной подготовки перед тем, как начать работать, и при каждом создании нового компонента ей тоже нужно настроиться. Вам нужно увидеть, как система "просыпается" и готовится к работе.
В классе SystemCore создайте статический блок инициализации. Этот блок будет выполнять "глобальную" настройку системы, которая происходит только один раз, при самой первой загрузке класса. Пусть он выводит сообщение вроде "Система: глобальная инициализация завершена.".
Затем, создайте нестатический блок инициализации. Этот блок, напротив, будет отвечать за "индивидуальную" подготовку каждого нового компонента SystemCore, который вы создаёте. Пусть он выводит сообщение "Компонент: индивидуальная настройка завершена.".
В главном методе main программы, чтобы наглядно увидеть порядок выполнения, создайте не один, а сразу два объекта SystemCore. Обратите внимание на то, в каком порядке появляются сообщения, чтобы понять логику запуска вашей сложной системы.

Требования:
•	В классе SystemCore должен быть реализован статический блок, который выполняется только один раз при первой загрузке класса.
•	Статический блок должен выводить на экран сообщение "Система: глобальная инициализация завершена.".
•	В классе SystemCore должен быть реализован нестатический блок, который выполняется при создании каждого нового объекта этого класса.
•	Нестатический блок должен выводить на экран сообщение "Компонент: индивидуальная настройка завершена." при каждом создании объекта SystemCore.
•	В методе main должно быть создано два объекта класса SystemCore для демонстрации порядка выполнения блоков инициализации.
•	Сначала должно появиться сообщение из статического блока (один раз), затем для каждого объекта — сообщение из нестатического блока (два раза).

public class Solution {
    public static void main(String[] args) {
        // Создаем два объекта SystemCore, чтобы увидеть порядок инициализации.
        // При первом создании класса сначала выполнится статический блок (один раз за всю программу)


        // При втором создании выполнится только нестатический блок (статический уже отработал).

    }
}

 */

public class Solution {
    public static void main(String[] args) {
        // Создаем два объекта SystemCore, чтобы увидеть порядок инициализации.
        // При первом создании класса сначала выполнится статический блок (один раз за всю программу)
        SystemCore mySystem = new SystemCore();

        // При втором создании выполнится только нестатический блок (статический уже отработал).
        SystemCore systemCore2 = new SystemCore();

    }
}

class SystemCore {
    static {
        System.out.println("Система: глобальная инициализация завершена.");
    }

    {
        System.out.println("Компонент: индивидуальная настройка завершена.");
    }


}

/*
public class Solution {
    public static void main(String[] args) {
        // Создаем два объекта SystemCore, чтобы увидеть порядок инициализации.
        // При первом создании класса сначала выполнится статический блок (один раз за всю программу)
        new SystemCore();

        // При втором создании выполнится только нестатический блок (статический уже отработал).
        new SystemCore();
    }
}

// Класс "сложной системы", в котором демонстрируется порядок инициализации
class SystemCore {
    // Статический блок: выполняется один раз при первой загрузке класса в память JVM
    static {
        System.out.println("Система: глобальная инициализация завершена.");
    }

    // Нестатический (инициализационный) блок: выполняется при каждом создании объекта
    {
        System.out.println("Компонент: индивидуальная настройка завершена.");
    }
}
 */
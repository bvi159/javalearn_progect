package ru.javarush.java.core.level25.task02;
/*
Персонализация питомцев: Присваиваем имена с super 🔼

Чтобы ваши виртуальные питомцы стали ещё более индивидуальными и узнаваемыми, пора дать им имена! Расширьте класс Animal, добавив в него новое поле name для хранения имени животного. Обязательно создайте конструктор для Animal, который будет принимать это имя и присваивать его соответствующему полю. Теперь вернитесь к вашему классу Cat.

Создайте для него собственный конструктор, который также будет принимать имя. Но здесь есть хитрость: вместо того чтобы самостоятельно присваивать имя, используйте ключевое слово super, чтобы вызвать конструктор родительского класса Animal и делегировать ему ответственность за инициализацию имени. Это позволит коту наследовать не только поведение, но и способ именования от своего родителя.

Требования:
•	В классе Animal должно быть объявлено новое поле name для хранения имени животного.
•	В классе Animal должен быть реализован конструктор, принимающий имя животного и присваивающий его полю name.
•	В классе Cat должен быть реализован собственный конструктор, принимающий имя животного.
•	В конструкторе класса Cat должен использоваться вызов super(name) для передачи имени в конструктор родительского класса Animal.

public class Solution {
    public static void main(String[] args) {
        // Создаем кота и передаем имя в конструктор Cat
        Cat cat = new Cat("Мурзик");

        // Проверим, что имя успешно инициализировалось через super(...)
        System.out.println(cat.name);
    }
}

 */
public class Solution {
    public static void main(String[] args) {
        // Создаем кота и передаем имя в конструктор Cat
        Cat cat = new Cat("Мурзик");

        // Проверим, что имя успешно инициализировалось через super(...)
        System.out.println(cat.name);
    }
}

class Animal {
    String name;

    Animal(String someName) {
        name = someName;
    }
}

class Cat extends Animal {
    Cat(String catName) {
        super(catName);
    }
}

/*
// Базовый класс Animal хранит имя животного
class Animal {
    String name; // поле для хранения имени

    // Конструктор принимает имя и присваивает его полю name
    Animal(String name) {
        this.name = name;
    }
}

// Класс Cat наследует Animal
class Cat extends Animal {
    // Конструктор Cat принимает имя и передает его родителю через super(...)
    Cat(String name) {
        super(name); // делегируем инициализацию имени базовому классу
    }
}
 */
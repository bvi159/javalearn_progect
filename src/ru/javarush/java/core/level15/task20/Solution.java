package ru.javarush.java.core.level15.task20;

/*
Автоматическое присвоение ID при регистрации пользователя 🧑‍💻

Вы проектируете систему регистрации пользователей. Каждый новый пользователь, независимо от того, как он регистрируется (быстрая регистрация или полная анкета), должен получить уникальный идентификатор. Возможно, на первом этапе ему присваивается временный ID, который затем может быть изменён.
Создайте класс UserProfile. В нём объявите приватное поле userIdentifier типа String. Добавьте нестатический блок инициализации. Внутри этого блока присвойте полю userIdentifier значение "DEFAULT_REG_ID" и выведите на экран сообщение "Временный ID пользователя присвоен.". Это действие должно выполняться для каждого нового объекта UserProfile.
Затем создайте два публичных конструктора для класса UserProfile:

Конструктор без параметров, который будет использоваться для "быстрой" регистрации. Внутри него выведите "Создан профиль без имени.".
Конструктор с одним параметром String initialName, который будет использоваться для "полной" регистрации и инициализации имени пользователя (если хотите, добавьте поле для имени). Внутри него выведите "Создан профиль с именем: " + initialName.
В классе Solution создайте два объекта UserProfile: один, используя конструктор без параметров, и второй, используя конструктор с именем (например, "Дарья").

Запустите программу. Вы должны заметить, что для каждого созданного объекта сообщение "Временный ID пользователя присвоен." выводится перед сообщением из соответствующего конструктора. Это демонстрирует, как нестатический блок обеспечивает общую инициализацию для всех конструкторов класса.

Требования:
•	В классе UserProfile должно быть объявлено приватное поле userIdentifier типа String.
•	В классе UserProfile должен быть реализован нестатический блок инициализации, который выполняется при создании каждого нового объекта.
•	В нестатическом блоке инициализации полю userIdentifier должно присваиваться значение "DEFAULT_REG_ID".
•	В нестатическом блоке инициализации должно выводиться сообщение "Временный ID пользователя присвоен.".
•	В классе UserProfile должен быть публичный конструктор без параметров, который выводит "Создан профиль без имени.".
•	В классе UserProfile должен быть публичный конструктор с одним параметром String initialName, который выводит "Создан профиль с именем: " + initialName.
•	В классе Solution должны быть созданы два объекта UserProfile: один с использованием конструктора без параметров, второй — с использованием конструктора с именем, например, "Дарья".
•	Для каждого объекта UserProfile сообщение "Временный ID пользователя присвоен." должно выводиться перед сообщением конструктора.

public class Solution {
    public static void main(String[] args) {
        // Создаем объект через "быструю" регистрацию (без параметров).
        // Сначала сработает нестатический блок (временный ID), затем сообщение конструктора.
        UserProfile fastRegistration = new UserProfile();

        // Создаем объект через "полную" регистрацию с именем "Дарья".
        // Снова сначала сработает блок инициализации, затем конструктор с параметром.
        UserProfile fullRegistration = new UserProfile("Дарья");
    }
}

 */

public class Solution {
    public static void main(String[] args) {
        // Создаем объект через "быструю" регистрацию (без параметров).
        // Сначала сработает нестатический блок (временный ID), затем сообщение конструктора.
        UserProfile fastRegistration = new UserProfile();

        // Создаем объект через "полную" регистрацию с именем "Дарья".
        // Снова сначала сработает блок инициализации, затем конструктор с параметром.
        UserProfile fullRegistration = new UserProfile("Дарья");
    }
}

class UserProfile {
    private String userIdentifier;

    {
        userIdentifier = "DEFAULT_REG_ID";
        System.out.println("Временный ID пользователя присвоен.");
    }

    public UserProfile() {
        System.out.println("Создан профиль без имени.");
    }
    public UserProfile(String initialName) {
        this.userIdentifier = initialName;
        System.out.println("Создан профиль с именем: " + userIdentifier);

    }
}

/*
public class UserProfile {
    // Приватное поле: уникальный идентификатор пользователя
    private String userIdentifier;

    // Необязательное поле для демонстрации "полной" регистрации с именем
    private String name;

    // Нестатический блок инициализации.
    // Он выполняется КАЖДЫЙ раз при создании нового объекта (перед вызовом любого конструктора).
    {
        userIdentifier = "DEFAULT_REG_ID"; // Присваиваем временный ID
        System.out.println("Временный ID пользователя присвоен.");
    }

    // Конструктор без параметров — "быстрая" регистрация
    public UserProfile() {
        System.out.println("Создан профиль без имени.");
    }

    // Конструктор с именем — "полная" регистрация
    public UserProfile(String initialName) {
        this.name = initialName; // Сохраняем имя (в рамках задачи это опционально)
        System.out.println("Создан профиль с именем: " + initialName);
    }
}
 */
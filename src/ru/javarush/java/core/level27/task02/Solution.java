package ru.javarush.java.core.level27.task02;

/*
Отслеживание инвентаря в игре 🎮

Вы разрабатываете игру, где игроки подбирают различные предметы. Вам важно знать, сколько всего предметов было подобрано, а сколько из них являются уникальными, чтобы избежать дубликатов в инвентаре.

Для отслеживания всех подобранных предметов, включая повторяющиеся, создайте переменную List<Integer>, используя ArrayList. Для учёта только уникальных предметов создайте переменную Set<Integer>, используя HashSet. В обе эти коллекции добавьте идентификаторы предметов: 3, 7, 3 и 9. После этого, чтобы понять состояние инвентаря, выведите на экран количество элементов в каждой из этих коллекций. Таким образом, вы сможете наглядно увидеть, сколько предметов было собрано всего, и сколько из них уникальны.

Требования:
•	Для хранения всех идентификаторов предметов (включая дубликаты) должна быть создана переменная типа List<Integer> на основе класса ArrayList.
•	Для хранения только уникальных идентификаторов предметов должна быть создана переменная типа Set<Integer> на основе класса HashSet.
•	В обе коллекции (List и Set) должны быть добавлены идентификаторы предметов: 3, 7, 3 и 9.
•	Программа должна вывести на экран количество элементов в каждой коллекции: сначала в List (общее количество предметов), затем в Set (количество уникальных предметов).
•	Для получения количества элементов в коллекциях должны быть использованы методы size() соответствующих коллекций.

import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Set;

public class Solution {
    public static void main(String[] args) {
        // List хранит все предметы, включая повторы


        // Set хранит только уникальные предметы (повторы игнорируются)


        // Добавляем идентификаторы 3, 7, 3, 9 в обе коллекции


        // Выводим количество: сначала общее (List), затем уникальное (Set)

    }
}

 */

import java.lang.reflect.Array;
import java.util.*;

public class Solution {
    public static void main(String[] args) {
        // List хранит все предметы, включая повторы
        List<Integer> things = new ArrayList<>();

        // Set хранит только уникальные предметы (повторы игнорируются)



        // Добавляем идентификаторы 3, 7, 3, 9 в обе коллекции
        things.addAll(Arrays.asList(3, 7, 3, 9));

        Set<Integer> uniqueThings = new HashSet<>(things);
        // Выводим количество: сначала общее (List), затем уникальное (Set)



        System.out.println(things.size());
        System.out.println(uniqueThings.size());

    }
}
/*
public class Solution {
    public static void main(String[] args) {
        // List хранит все предметы, включая повторы
        List<Integer> allItems = new ArrayList<>();

        // Set хранит только уникальные предметы (повторы игнорируются)
        Set<Integer> uniqueItems = new HashSet<>();

        // Добавляем идентификаторы 3, 7, 3, 9 в обе коллекции
        int[] ids = {3, 7, 3, 9};
        for (int id : ids) {
            allItems.add(id);    // в списке дубликаты сохраняются
            uniqueItems.add(id); // в множестве дубликаты отбрасываются
        }

        // Выводим количество: сначала общее (List), затем уникальное (Set)
        System.out.println(allItems.size());
        System.out.println(uniqueItems.size());
    }
}

 */
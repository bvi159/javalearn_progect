package ru.javarush.java.core.level27.task06;
/*
Имитация работы принтера и истории действий 🖨️🔄

Вы разрабатываете универсальное приложение, которое должно демонстрировать два основных принципа обработки данных: «первым пришёл — первым обслужен» (как в очереди печати) и «последним пришёл — первым обслужен» (как в истории отмены действий).
Для имитации очереди печати создайте Queue<String> на базе ArrayDeque. Добавьте в неё названия документов для печати: "Кот", "Пёс", "Мышь". Затем извлеките и выведите все эти элементы в том порядке, в котором они должны быть распечатаны.
Далее, для демонстрации истории отмены действий (например, в текстовом редакторе), создайте Deque<Character> на базе ArrayDeque, который будет работать как стек. Добавьте в этот стек символы, обозначающие последние действия: 'X', 'Y', 'Z' (именно в таком порядке). Теперь, чтобы имитировать отмену действий, снимите и выведите все элементы стека по одному, показывая, какое действие было отменено последним и так далее.
В конце, чтобы чётко разделить результаты, выведите подпись "Очередь:", затем все элементы очереди, каждый на отдельной строке. После этого выведите подпись "Стек:", и далее все элементы стека, также каждый на отдельной строке.

Требования:
•	В задаче должны быть использованы коллекции на базе ArrayDeque для реализации очереди и стека.
•	Необходимо создать Queue<String> на базе ArrayDeque и добавить в неё элементы "Кот", "Пёс", "Мышь" в указанном порядке.
•	Все элементы очереди должны быть извлечены и выведены в том порядке, в котором они были добавлены (FIFO).
•	Необходимо создать Deque<Character> на базе ArrayDeque и добавить в него символы 'X', 'Y', 'Z' в указанном порядке, чтобы структура работала как стек.
•	Все элементы стека должны быть извлечены и выведены в порядке LIFO (последний добавленный выходит первым).
•	Сначала должна быть выведена строка "Очередь:", затем все элементы очереди (каждый на новой строке), далее строка "Стек:", после чего все элементы стека (каждый на новой строке).

import java.util.ArrayDeque;
import java.util.Deque;
import java.util.Queue;

public class Solution {
    public static void main(String[] args) {
        // Очередь печати на базе ArrayDeque (FIFO)


        // Стек истории действий на базе ArrayDeque (LIFO)

        // Используем push для добавления на вершину стека


        // Вывод очереди: сначала заголовок, затем извлекаем (poll) элементы в порядке FIFO


        // Вывод стека: сначала заголовок, затем снимаем (pop) элементы в порядке LIFO

    }
}

 */

import java.util.*;

public class Solution {
    public static void main(String[] args) {
        // Очередь печати на базе ArrayDeque (FIFO)
        Queue<String> myQue = new ArrayDeque<>();

//        myQue.addAll(Arrays.asList("Кот", "Пёс", "Мышь"));
        myQue.addAll(List.of("Кот", "Пёс", "Мышь"));

        System.out.println("Очередь:");
        while (!myQue.isEmpty()) {
            System.out.println(myQue.poll());
        }

//        for (String s : myQue) {
//            System.out.println(s);
//        }

        Deque<Character> myStack = new ArrayDeque<>();

//        mySteck.addAll(Arrays.asList('X', 'Y', 'Z'));
        myStack.push('X');
        myStack.push('Y');
        myStack.push('Z');

        System.out.println("Стек:");
        while (!myStack.isEmpty()) {

            System.out.println(myStack.pop());
        }

        // Стек истории действий на базе ArrayDeque (LIFO)


        // Используем push для добавления на вершину стека


        // Вывод очереди: сначала заголовок, затем извлекаем (poll) элементы в порядке FIFO


        // Вывод стека: сначала заголовок, затем снимаем (pop) элементы в порядке LIFO

    }
}

/*

public class Solution {
    public static void main(String[] args) {
        // Очередь печати на базе ArrayDeque (FIFO)
        Queue<String> printQueue = new ArrayDeque<>();
        printQueue.add("Кот");
        printQueue.add("Пёс");
        printQueue.add("Мышь");

        // Стек истории действий на базе ArrayDeque (LIFO)
        Deque<Character> undoStack = new ArrayDeque<>();
        // Используем push для добавления на вершину стека
        undoStack.push('X');
        undoStack.push('Y');
        undoStack.push('Z');

        // Вывод очереди: сначала заголовок, затем извлекаем (poll) элементы в порядке FIFO
        System.out.println("Очередь:");
        while (!printQueue.isEmpty()) {
            System.out.println(printQueue.poll()); // poll удаляет голову очереди и возвращает её
        }

        // Вывод стека: сначала заголовок, затем снимаем (pop) элементы в порядке LIFO
        System.out.println("Стек:");
        while (!undoStack.isEmpty()) {
            System.out.println(undoStack.pop()); // pop снимает элемент с вершины стека
        }
    }
}

 */
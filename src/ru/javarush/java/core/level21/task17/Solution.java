package ru.javarush.java.core.level21.task17;

import org.jetbrains.annotations.NotNull;

/*
Сравнение числовых "контейнеров" 📦

Представьте, что вы разрабатываете специальный "умный" контейнер для чисел, который умеет автоматически сравнивать себя с другими такими же контейнерами. Это нужно для того, чтобы, например, сортировать их или находить наибольший/наименьший.

Создайте класс NumberBox, у которого будет одно целочисленное поле для хранения значения. Для этого класса реализуйте интерфейс Comparable<NumberBox>, чтобы он мог сравнивать себя с другими NumberBox'ами. Сравнение должно происходить по значению хранящегося внутри числа.

В основной части вашей программы создайте два объекта NumberBox с разными значениями (например, 10 и 5). Затем вызовите метод compareTo() на одном из них, передав другой, и выведите полученный результат на экран, чтобы увидеть, как они соотносятся.

Требования:
•	Должен быть создан публичный класс с именем NumberBox.
•	Класс NumberBox должен содержать приватное целочисленное поле для хранения значения.
•	Класс NumberBox должен реализовывать интерфейс Comparable<NumberBox>.
•	В классе NumberBox должен быть реализован публичный метод compareTo(NumberBox other), который сравнивает текущий объект с другим объектом NumberBox по значению их числовых полей.
•	Метод compareTo должен возвращать отрицательное число, если значение текущего объекта меньше значения переданного объекта; положительное число, если больше; и ноль, если значения равны.
•	В основной части программы должны быть созданы два объекта NumberBox с разными значениями (например, 10 и 5).
•	В основной части программы должен быть вызван метод compareTo на одном из объектов NumberBox, передав в качестве аргумента второй объект, а результат должен быть выведен на экран.

public class Solution {
    public static void main(String[] args) {
        // Создаем два контейнера с разными значениями
        NumberBox boxA = new NumberBox(10);
        NumberBox boxB = new NumberBox(5);

        // Сравниваем их: положительное число — boxA больше boxB; отрицательное — меньше; ноль — равны
        int result = boxA.compareTo(boxB);

        // Выводим результат сравнения на экран
        System.out.println(result);
    }
}

 */
public class Solution {
    public static void main(String[] args) {
        // Создаем два контейнера с разными значениями
        NumberBox boxA = new NumberBox(10);
        NumberBox boxB = new NumberBox(5);

        // Сравниваем их: положительное число — boxA больше boxB; отрицательное — меньше; ноль — равны
        int result = boxA.compareTo(boxB);

        // Выводим результат сравнения на экран
        System.out.println(result);
    }
}

/*

// Публичный класс-контейнер для числа, который умеет сравнивать себя с другими NumberBox
public class NumberBox implements Comparable<NumberBox> {
    // Приватное поле для хранения значения
    private final int value;

    // Простой конструктор для инициализации значения
    public NumberBox(int value) {
        this.value = value;
    }

    @Override
    public int compareTo(NumberBox other) {
        // Сравниваем по значению:
        // отрицательное — если this < other, положительное — если this > other, 0 — если равны
        return Integer.compare(this.value, other.value);
    }
}

 */
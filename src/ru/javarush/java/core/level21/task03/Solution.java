package ru.javarush.java.core.level21.task03;
/*
Рейтинг спортсменов по "силе удара" 🥊

В необычном спортивном соревновании очки начисляются не просто за результат, а за "силу удара", которая может быть как положительной, так и отрицательной в зависимости от направления, но для рейтинга важна лишь абсолютная величина. Чем больше абсолютное значение, тем выше место.
Вам необходимо отсортировать список результатов спортсменов. Для этого создайте специальный компаратор, который будет сравнивать два целых числа исключительно по их абсолютному значению, игнорируя знак. Используйте лямбда-выражение для быстрого определения логики сравнения. Затем примените этот компаратор к списку текущих результатов: -5, 3, -2, 7, 0, чтобы получить отсортированный рейтинг. В конце выведите получившийся список на экран, чтобы увидеть, кто оказался "сильнее".

Требования:
•	Компаратор для сравнения целых чисел по абсолютному значению должен быть реализован с помощью лямбда-выражения.
•	Компаратор должен сравнивать два целых числа, учитывая только их абсолютные значения, игнорируя знак.
•	Список целых чисел должен быть отсортирован с помощью созданного компаратора, чтобы элементы располагались по возрастанию абсолютного значения.
•	Для сортировки должен использоваться список: -5, 3, -2, 7, 0.
•	После сортировки программа должна вывести получившийся список на экран.

public class Solution {
    public static void main(String[] args) {
        // Исходный список результатов спортсменов


        // Компаратор-лямбда: сравниваем числа только по их абсолютному значению (знак игнорируется)


        // Сортируем список по возрастанию абсолютной величины


        // Выводим отсортированный список

    }
}

 */
import java.util.ArrayList;
import java.util.Arrays;
import java.util.Comparator;
import java.util.List;

public class Solution {
    public static void main(String[] args) {
        // Исходный список результатов спортсменов
        List<Integer> resultList = new ArrayList<>(List.of(-5, 3, -2, 7, 0));
        // Компаратор-лямбда: сравниваем числа только по их абсолютному значению (знак игнорируется)
        Comparator<Integer> listCompare = (i1, i2) -> Integer.compare(Math.abs(i1), Math.abs(i2));

        // Сортируем список по возрастанию абсолютной величины
        resultList.sort(listCompare);

        // Выводим отсортированный список
        System.out.println(resultList);

    }
}

/*
public class Solution {
    public static void main(String[] args) {
        // Исходный список результатов спортсменов
        List<Integer> results = new ArrayList<>(Arrays.asList(-5, 3, -2, 7, 0));

        // Компаратор-лямбда: сравниваем числа только по их абсолютному значению (знак игнорируется)
        Comparator<Integer> byAbsolute = (a, b) -> Integer.compare(Math.abs(a), Math.abs(b));

        // Сортируем список по возрастанию абсолютной величины
        results.sort(byAbsolute);

        // Выводим отсортированный список
        System.out.println(results); // Ожидаемо: [0, -2, 3, -5, 7]
    }
}
 */
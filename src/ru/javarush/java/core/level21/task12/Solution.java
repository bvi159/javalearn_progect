package ru.javarush.java.core.level21.task12;
/*
Интеграция модулей из разных систем 🧩

Представьте, что вы объединяете функциональность двух независимых модулей A и B в одну новую общую систему C. Оба старых модуля имеют метод show(), но выводят разные сообщения. Ваша задача — создать новую систему C, которая при вызове своего show() метода сможет последовательно вызвать show() из модуля A, а затем show() из модуля B.
Создайте два интерфейса: A с default-методом void show(), который выводит "A", и B с default-методом void show(), который выводит "B". Затем создайте класс C, который будет реализовывать оба этих интерфейса. В классе C вам нужно реализовать свой собственный метод show() таким образом, чтобы он сначала явно вызывал show() из интерфейса A, а затем show() из интерфейса B. В основной программе создайте объект C и вызовите его метод show(), чтобы увидеть результат последовательного вызова.

Требования:
•	Необходимо создать интерфейс A, в котором определён default-метод void show(), выводящий "A".
•	Необходимо создать интерфейс B, в котором определён default-метод void show(), выводящий "B".
•	Класс C должен реализовывать оба интерфейса — A и B.
•	В классе C необходимо переопределить метод show() для разрешения конфликта default-методов.
•	В переопределённом методе show() класса C сначала должен явно вызываться метод show() из интерфейса A, затем метод show() из интерфейса B.
•	В основной программе необходимо создать объект типа C и вызвать у него метод show().

public class Solution {
    public static void main(String[] args) {
        C c = new C();
        c.show(); // Должно вывести: A (с новой строки) затем B
    }
}

 */

// Демонстрация разрешения конфликта default-методов при множественной реализации интерфейсов.
public class Solution {
    public static void main(String[] args) {
        C c = new C();
        c.show(); // Должно вывести: A (с новой строки) затем B
    }
}

interface A {
    default void show() {
        System.out.println("A");
    }
}

interface B {
    default void show() {
        System.out.println("B");
    }
}

class C implements A, B {
    @Override
    public void show() {
        A.super.show();
        B.super.show();
    }

}

/*
// Демонстрация разрешения конфликта default-методов при множественной реализации интерфейсов.
public class Solution {
    public static void main(String[] args) {
        C c = new C();
        c.show(); // Должно вывести: A (с новой строки) затем B
    }
}

// Интерфейс A с default-методом show(), печатающим "A"
interface A {
    default void show() {
        System.out.println("A");
    }
}

// Интерфейс B с default-методом show(), печатающим "B"
interface B {
    default void show() {
        System.out.println("B");
    }
}

// Класс C реализует оба интерфейса A и B
class C implements A, B {
    @Override
    public void show() {
        // Разрешаем конфликт одинаковых default-методов:
        // явно вызываем реализации из каждого интерфейса
        A.super.show(); // сначала метод из A
        B.super.show(); // затем метод из B
    }
}
 */
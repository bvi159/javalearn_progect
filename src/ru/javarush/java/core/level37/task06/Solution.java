package ru.javarush.java.core.level37.task06;
/*
Атлас цифровых языков: Обзор поддерживаемых кодировок 🗺️
Представьте себя лингвистом-исследователем, который оказался в самом сердце гигантской библиотеки, где хранятся все известные методы записи информации — от древних иероглифов до современных цифровых стандартов.

Ваша цель — составить полный атлас всех availableCharsets (кодировок), которые понимает ваша Java-виртуальная машина. Для каждой обнаруженной charsetAlias покажите её официальное имя на экране, как будто вы перечисляете все уникальные "языки", которые ваш универсальный переводчик способен распознать и использовать.

Требования:
•	Программа должна использовать метод java.nio.charset.Charset.availableCharsets() для получения списка всех поддерживаемых кодировок.
•	Программа должна перебрать все элементы, возвращаемые availableCharsets(), чтобы обработать каждую доступную кодировку.
•	Для каждой найденной кодировки программа должна вывести её официальное имя (canonical name) на экран.
•	Программа должна корректно использовать типы данных из пакета java.nio.charset, например, Map<String, Charset> для хранения списка кодировок.
•	Программа не должна использовать заранее заданный список имён кодировок — только те, что реально возвращаются JVM.

import java.nio.charset.Charset;
import java.util.Map;

public class Solution {
    public static void main(String[] args) {
        // Получаем от JVM все доступные кодировки.
        // Метод возвращает отсортированную карту: ключ — каноническое (официальное) имя, значение — объект Charset.


        // Перебираем все элементы карты (все поддерживаемые JVM кодировки)

    }
}

 */

import java.nio.charset.Charset;

public class Solution {
    public static void main(String[] args) {
        // Получаем от JVM все доступные кодировки.
        // Метод возвращает отсортированную карту: ключ — каноническое (официальное) имя, значение — объект Charset.
        System.out.println("У меня есть такие кодировке:");
        Charset.availableCharsets().forEach((name, charset) ->
        {
            System.out.println(name + " - " + charset);
        });
//        Charset.availableCharsets().forEach((name, charset) -> System.out.println(name));

        // Перебираем все элементы карты (все поддерживаемые JVM кодировки)

    }
}
/*
public class Solution {
    public static void main(String[] args) {
        // Получаем от JVM все доступные кодировки.
        // Метод возвращает отсортированную карту: ключ — каноническое (официальное) имя, значение — объект Charset.
        Map<String, Charset> available = Charset.availableCharsets();

        // Перебираем все элементы карты (все поддерживаемые JVM кодировки)
        for (Map.Entry<String, Charset> entry : available.entrySet()) {
            String canonicalName = entry.getKey(); // Каноническое (официальное) имя кодировки
            // Выводим официальное имя кодировки на экран
            System.out.println(canonicalName);
        }
    }
}
 */
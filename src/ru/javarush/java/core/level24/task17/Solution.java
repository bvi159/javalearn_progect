package ru.javarush.java.core.level24.task17;
/*
Новостной агрегатор: Автоматическое чтение заголовка статьи
Вы пишете программу для автоматического новостного агрегатора, которая должна быстро считывать заголовки статей из файлов. Крайне важно, чтобы после прочтения файла все ресурсы были аккуратно закрыты, даже если произошла ошибка.
Ваша задача — разработать механизм, который будет читать первую строку файла под названием "article_text.txt" и выводить её на экран. Используйте для этого специальную конструкцию try-with-resources. Это гарантирует, что даже если файл не найден или произошла другая ошибка при чтении, используемые ресурсы будут автоматически и корректно закрыты.
Если по какой-либо причине (например, файл не существует или есть проблемы с доступом) произойдёт ошибка при чтении файла, ваша программа должна перехватить эту ситуацию и вывести сообщение "Ошибка чтения статьи!". Таким образом, ваш агрегатор будет надёжно работать, обеспечивая чистоту ресурсов.

Требования:
•	В программе должен быть использован блок try-with-resources для автоматического закрытия ресурсов при работе с файлом.
•	Программа должна прочитать только первую строку из файла "article_text.txt".
•	Прочитанная первая строка должна быть выведена на экран.
•	В случае возникновения любой ошибки при чтении файла (например, файл не найден или нет доступа), программа должна перехватить исключение.
•	Если при чтении файла возникла ошибка, программа должна вывести сообщение "Ошибка чтения статьи!" на экран.
public class Solution {
    public static void main(String[] args) {
        // Используем try-with-resources, чтобы поток автоматически закрылся

            // Читаем только первую строку файла — заголовок

            // На любую ошибку чтения выводим сообщение об ошибке


    }
}
 */

import java.io.BufferedReader;
import java.io.FileReader;
import java.io.IOException;

public class Solution {
    public static void main(String[] args) {
        // Используем try-with-resources, чтобы поток автоматически закрылся
        try (BufferedReader myNews = new BufferedReader(new FileReader("article_text.txt"))) {
            String firstLine = myNews.readLine();
            System.out.println(firstLine);
        } catch (IOException e) {
            System.out.println("Ошибка чтения статьи!");
        }
            // Читаем только первую строку файла — заголовок

            // На любую ошибку чтения выводим сообщение об ошибке


    }
}

/*
public class Solution {
    public static void main(String[] args) {
        // Используем try-with-resources, чтобы поток автоматически закрылся
        try (BufferedReader reader = new BufferedReader(new FileReader("article_text.txt"))) {
            // Читаем только первую строку файла — заголовок
            String firstLine = reader.readLine();
            if (firstLine != null) {
                System.out.println(firstLine);
            }
        } catch (Exception e) {
            // На любую ошибку чтения выводим сообщение об ошибке
            System.out.println("Ошибка чтения статьи!");
        }
    }
}

 */
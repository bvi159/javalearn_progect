package ru.javarush.java.core.level24.task13;
/*
Финансовый отчёт: Точная диагностика проблем с данными

Вы разрабатываете критически важную систему для финансового отдела, которая должна обрабатывать ежедневные транзакции из файла. Если что-то пойдёт не так, бухгалтеры должны получить максимально чёткую информацию о том, что случилось: файл не найден, возникли общие проблемы при чтении, или же данные внутри файла оказались "испорченными".
Ваша задача — написать программу, которая попытается открыть файл под названием "daily_transactions.txt". Затем она попробует прочитать первую строку из этого файла и преобразовать её в целое число, которое могло бы быть суммой транзакций.
Чтобы чётко сообщать, что именно пошло не так, используйте несколько отдельных catch-блоков:
Если файл "daily_transactions.txt" не будет найден (FileNotFoundException), ваша программа должна вывести сообщение: "Файл транзакций не найден!".
Если произойдёт любая другая ошибка при попытке прочитать файл (IOException), выведите: "Ошибка чтения данных из файла!".
Если же первая строка файла окажется не числом, а каким-то бессмысленным текстом (NumberFormatException), программа должна сообщить: "В файле обнаружены некорректные финансовые данные!".
Таким образом, вы создадите надёжный инструмент для финансового отдела, который позволит им точно знать, как реагировать на каждую конкретную проблему с отчётом.

Требования:
•	Программа должна пытаться открыть файл с именем "daily_transactions.txt" для чтения.
•	Программа должна считать первую строку из файла "daily_transactions.txt".
•	Программа должна попытаться преобразовать первую строку файла в целое число.
•	Если файл "daily_transactions.txt" не найден, программа должна вывести сообщение: "Файл транзакций не найден!".
•	Если при чтении файла возникает любая другая IOException, программа должна вывести сообщение: "Ошибка чтения данных из файла!".
•	Если первая строка файла не может быть преобразована в число (NumberFormatException), программа должна вывести сообщение: "В файле обнаружены некорректные финансовые данные!".
•	Для каждого типа ошибки (FileNotFoundException, IOException, NumberFormatException) должны быть реализованы отдельные блоки catch с соответствующими сообщениями.

public class Solution {
    public static void main(String[] args) {
        // try-with-resources автоматически закроет файл даже при ошибках

            // Читаем первую строку из файла


            // Пустой файл или отсутствие первой строки считаем некорректными данными


            // Пытаемся преобразовать строку в целое число


            // Успешный сценарий: по условию ничего не выводим
            // (можно было бы вывести total, но этого не требуют)

            // Отдельный план для ситуации, когда файла нет

            // Отдельный план для некорректных данных в первой строке

            // Любые другие проблемы ввода-вывода при чтении файла

    }
}

 */
import java.io.*;

public class Solution {
    public static void main(String[] args) {
        // try-with-resources автоматически закроет файл даже при ошибках
//        try (BufferedReader myBuff = new BufferedReader(new FileReader("daily_transactions.txt"))){
        try (InputStream inputStream = Solution.class.getResourceAsStream("daily_transactions.txt");
             BufferedReader myBuff = new BufferedReader(new InputStreamReader(inputStream))) {
           int transactionAmmount = Integer.parseInt(myBuff.readLine());

        } catch (FileNotFoundException e) {
            System.out.println("Файл транзакций не найден!");
            //throw new RuntimeException(e);
        } catch (IOException e) {
            System.out.println("Ошибка чтения данных из файла!");
            //throw new RuntimeException(e);
        } catch (NumberFormatException e) {
            System.out.println("В файле обнаружены некорректные финансовые данные!");
            //throw new RuntimeException(e);
        }

        // Читаем первую строку из файла


            // Пустой файл или отсутствие первой строки считаем некорректными данными


            // Пытаемся преобразовать строку в целое число


            // Успешный сценарий: по условию ничего не выводим
            // (можно было бы вывести total, но этого не требуют)

            // Отдельный план для ситуации, когда файла нет

            // Отдельный план для некорректных данных в первой строке

            // Любые другие проблемы ввода-вывода при чтении файла

    }
}

/*
public class Solution {
    public static void main(String[] args) {
        // try-with-resources автоматически закроет файл даже при ошибках
        try (BufferedReader reader = new BufferedReader(new FileReader("daily_transactions.txt"))) {
            // Читаем первую строку из файла
            String firstLine = reader.readLine();

            // Пустой файл или отсутствие первой строки считаем некорректными данными
            if (firstLine == null) {
                throw new NumberFormatException("Пустой файл");
            }

            // Пытаемся преобразовать строку в целое число
            int total = Integer.parseInt(firstLine.trim());

            // Успешный сценарий: по условию ничего не выводим
            // (можно было бы вывести total, но этого не требуют)
        } catch (FileNotFoundException e) {
            // Отдельный план для ситуации, когда файла нет
            System.out.println("Файл транзакций не найден!");
        } catch (NumberFormatException e) {
            // Отдельный план для некорректных данных в первой строке
            System.out.println("В файле обнаружены некорректные финансовые данные!");
        } catch (IOException e) {
            // Любые другие проблемы ввода-вывода при чтении файла
            System.out.println("Ошибка чтения данных из файла!");
        }
    }
}
 */
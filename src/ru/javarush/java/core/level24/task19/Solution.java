package ru.javarush.java.core.level24.task19;

/*
Научная лаборатория: Документирование потенциальных сбоев в данных

Вы разрабатываете систему для ввода результатов научных измерений. Некоторые измерения представляют собой числа, но иногда учёные могут случайно ввести текст вместо цифр. Вам, как разработчику, важно не только обрабатывать такие ситуации, но и ясно обозначить для других программистов, какие "подводные камни" могут встретиться при использовании вашего кода.

Создайте метод, назовите его parseMeasurementValue, который будет принимать на вход строку с данными измерения. Внутри этого метода используйте стандартный механизм для преобразования строки в целое число. Если строка не может быть преобразована в число (например, содержит буквы), это вызовет NumberFormatException.

Самое важное: оформите Javadoc-комментарии для вашего метода parseMeasurementValue. В этих комментариях, используя специальную аннотацию @throws, явно укажите, что данный метод может "выбросить" NumberFormatException, если входная строка не является корректным числом.

В главной части вашей программы вызовите parseMeasurementValue, передав ему строку "123abc", которая заведомо не является числом. Если возникнет исключение (что и произойдёт), перехватите его и выведите на экран сообщение "Ошибка преобразования числа: введены некорректные данные", чтобы пользователь понял, почему его ввод не был принят.

Требования:
•	Необходимо создать метод с именем parseMeasurementValue, который принимает строковый параметр.
•	Внутри метода parseMeasurementValue должна использоваться стандартная операция преобразования строки в целое число (например, Integer.parseInt).
•	Для метода parseMeasurementValue необходимо оформить Javadoc-комментарий с аннотацией @throws, в котором указано, что метод может выбросить NumberFormatException при некорректном формате строки.
•	В основной части программы вызвать метод parseMeasurementValue с аргументом "123abc" и обработать возможное исключение NumberFormatException с помощью блока try-catch.
•	В случае возникновения NumberFormatException в блоке catch вывести на экран сообщение: "Ошибка преобразования числа: введены некорректные данные".

public class Solution {
    public static void main(String[] args) {

            // Передаём заведомо некорректную строку: содержит буквы и не может быть преобразована в int


            // В этой задаче выполнение сюда не дойдёт, но оставим строку для полноты примера


            // Перехватываем ошибку преобразования и сообщаем пользователю понятным текстом


    }

    //    Преобразует строковое значение измерения в целое число.
    public static int parseMeasurementValue(String measurement) {
        // Стандартный способ преобразовать строку в int; при некорректном формате бросает NumberFormatException

    }
}

 */

public class Solution {
    public static void main(String[] args) {
        try {
            // Передаём заведомо некорректную строку: содержит буквы и не может быть преобразована в int
            int value = parseMeasurementValue("123abc");

            // В этой задаче выполнение сюда не дойдёт, но оставим строку для полноты примера
            System.out.println("Успешно преобразовано: " + value);
        } catch (NumberFormatException e) {
            // Перехватываем ошибку преобразования и сообщаем пользователю понятным текстом
            System.out.println("Ошибка преобразования числа: введены некорректные данные");
        }
    }

    /**
     * Преобразует строковое значение измерения в целое число.
     *
     * @param measurement строковое значение измерения
     * @return числовое значение измерения
     * @throws NumberFormatException если произошла ошибка преобразования строки в число
     */
    public static int parseMeasurementValue(String measurement) {
        // Стандартный способ преобразовать строку в int; при некорректном формате бросает NumberFormatException
        return Integer.parseInt(measurement);
    }
}


/*
public class Solution {
    public static void main(String[] args) {
        try {
            // Передаём заведомо некорректную строку: содержит буквы и не может быть преобразована в int
            int value = parseMeasurementValue("123abc");

            // В этой задаче выполнение сюда не дойдёт, но оставим строку для полноты примера
            System.out.println("Успешно преобразовано: " + value);
        } catch (NumberFormatException e) {
            // Перехватываем ошибку преобразования и сообщаем пользователю понятным текстом
            System.out.println("Ошибка преобразования числа: введены некорректные данные");
        }
    }

    / **
     * Преобразует строковое значение измерения в целое число.
     *
     * @param measurement строковое значение измерения
     * @return числовое значение измерения
     * @throws NumberFormatException если произошла ошибка преобразования строки в число
     * /
public static int parseMeasurementValue(String measurement) {
    // Стандартный способ преобразовать строку в int; при некорректном формате бросает NumberFormatException
    return Integer.parseInt(measurement);
}
}
 */
package ru.javarush.java.core.level24.task05;
/*
Страж ворот игры: Проверка игрового счёта

Вы разрабатываете систему подсчёта очков для вашей новой увлекательной игры. Но есть одно важное правило: счёт игрока не может быть "слишком мал", иначе это может испортить весь игровой процесс. Вам нужна особая проверка, которая "крикнет" об ошибке, если кто-то попытается ввести недопустимое значение.
Ваша миссия — создать специальный метод, назовите его checkPlayerScore. Этот метод должен принимать на вход целое число, представляющее предполагаемый счёт игрока. Если это число меньше 10, метод должен "выбросить" ваше собственное, специально созданное исключение, например, ScoreTooLowException, которое будет наследником обычного Exception. Это исключение будет сигнализировать о том, что "Счёт игрока слишком низкий!". Если же счёт в порядке (10 или больше), метод просто спокойно продолжит свою работу.
Таким образом, вы создадите надёжный страж ворот, который будет гарантировать, что счёт игрока всегда будет значимым и ваша игра не столкнётся с нелепыми значениями.

Требования:
•	Необходимо создать собственный класс исключения с именем ScoreTooLowException, который должен наследоваться от класса Exception.
•	Класс ScoreTooLowException должен иметь конструктор, принимающий строку с сообщением об ошибке.
•	В программе должен быть реализован метод checkPlayerScore, принимающий один параметр типа int.
•	Метод checkPlayerScore должен проверять, что переданное значение меньше 10.
•	Если переданное значение меньше 10, метод checkPlayerScore должен выбрасывать исключение ScoreTooLowException с сообщением "Счёт игрока слишком низкий!".
•	Если переданное значение 10 или больше, метод checkPlayerScore не должен выбрасывать никаких исключений и должен завершаться корректно.

public class Solution {
    public static void main(String[] args) {
        // Небольшая демонстрация работы "стража ворот"

        try {
            // Пробуем слишком маленький счёт — ожидаем исключение
            checkPlayerScore(5);
            System.out.println("Этот текст не будет выведен");
        } catch (ScoreTooLowException e) {
            System.out.println(e.getMessage()); // Выведет: "Счёт игрока слишком низкий!"
        }

        try {
            // Пробуем корректный счёт — исключений быть не должно
            checkPlayerScore(12);
            System.out.println("Счёт 12 принят");
        } catch (ScoreTooLowException e) {
            System.out.println(e.getMessage());
        }
    }

    // Метод-страж: проверяет счёт и бросает собственное исключение при некорректном значении
    public static void checkPlayerScore(int score) throws ScoreTooLowException {

            // Сообщение должно быть строго таким, как в требованиях

        // Если счёт 10 или больше — метод завершается без исключений
    }
}

 */
public class Solution {
    public static void main(String[] args) {
        // Небольшая демонстрация работы "стража ворот"

        try {
            // Пробуем слишком маленький счёт — ожидаем исключение
            checkPlayerScore(5);
            System.out.println("Этот текст не будет выведен");
        } catch (ScoreTooLowException e) {
            System.out.println(e.getMessage()); // Выведет: "Счёт игрока слишком низкий!"
        }

        try {
            // Пробуем корректный счёт — исключений быть не должно
            checkPlayerScore(12);
            System.out.println("Счёт 12 принят");
        } catch (ScoreTooLowException e) {
            System.out.println(e.getMessage());
        }
    }

    // Метод-страж: проверяет счёт и бросает собственное исключение при некорректном значении
    public static void checkPlayerScore(int score) throws ScoreTooLowException {
        if (score < 10) {
            throw new ScoreTooLowException("Счёт игрока слишком низкий!");
        }
            // Сообщение должно быть строго таким, как в требованиях

        // Если счёт 10 или больше — метод завершается без исключений
    }
}

/*
   // Метод-страж: проверяет счёт и бросает собственное исключение при некорректном значении
    public static void checkPlayerScore(int score) throws ScoreTooLowException {
        if (score < 10) {
            // Сообщение должно быть строго таким, как в требованиях
            throw new ScoreTooLowException("Счёт игрока слишком низкий!");
        }
        // Если счёт 10 или больше — метод завершается без исключений
    }
}
 */
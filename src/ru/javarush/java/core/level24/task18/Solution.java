package ru.javarush.java.core.level24.task18;

/*
Магический артефакт: Самостоятельное отключение

Представьте, что вы управляете очень деликатным магическим артефактом, который нужно тщательно "отключать" после использования, иначе могут быть непредсказуемые последствия. Ваша цель — убедиться, что этот артефакт самостоятельно завершает работу, как только его перестают использовать, даже если в процессе работы возникнут проблемы.

Создайте класс, назовите его MagicArtifact. Этот класс должен быть умным и реализовывать специальный интерфейс AutoCloseable. В методе close() этого класса (который вызывается автоматически при закрытии) выведите сообщение: "Магический артефакт безопасно отключён".

В главной части вашей программы (методе main) используйте конструкцию try-with-resources для создания объекта MagicArtifact. Внутри блока try выведите сообщение "Идёт работа с магическим артефактом". После завершения блока try, программа должна завершить свою работу, а в консоли вы обязательно должны увидеть сообщение о том, что магический артефакт был безопасно отключён, подтверждая его способность к самостоятельному "завершению дел".

Требования:
•	Класс MagicArtifact должен реализовывать интерфейс AutoCloseable.
•	В классе MagicArtifact должен быть реализован метод close(), который выводит на экран сообщение: "Магический артефакт безопасно отключён".
•	В методе main объект класса MagicArtifact должен создаваться внутри конструкции try-with-resources.
•	Внутри блока try-with-resources должно выводиться сообщение: "Идёт работа с магическим артефактом".
•	После выхода из блока try-with-resources в консоли обязательно должно появиться сообщение о безопасном отключении магического артефакта.

class MagicArtifact {

    @Override
    public void close() {
        // Сообщение, подтверждающее автоматическое отключение артефакта
        System.out.println("Магический артефакт безопасно отключён");
    }
}

public class Solution {
    public static void main(String[] args) {
        // try-with-resources гарантирует автоматический вызов close()
        // даже если внутри блока произойдет исключение

            System.out.println("Идёт работа с магическим артефактом");
            // Здесь могла бы быть любая логика работы с артефактом

        // К этому моменту артефакт уже закрыт, сообщение выведено
    }
}


 */

// Класс ресурса, который нужно корректно закрывать
class MagicArtifact implements AutoCloseable {

    @Override
    public void close() {
        // Сообщение, подтверждающее автоматическое отключение артефакта
        System.out.println("Магический артефакт безопасно отключён");
    }
}

public class Solution {
    public static void main(String[] args) {
        // try-with-resources гарантирует автоматический вызов close()
        try (MagicArtifact bawl = new MagicArtifact()) {
            System.out.println("Идёт работа с магическим артефактом");
        }


        // даже если внутри блока произойдет исключение


            // Здесь могла бы быть любая логика работы с артефактом

        // К этому моменту артефакт уже закрыт, сообщение выведено
    }
}

/*
// Класс ресурса, который нужно корректно закрывать
class MagicArtifact implements AutoCloseable {

    @Override
    public void close() {
        // Сообщение, подтверждающее автоматическое отключение артефакта
        System.out.println("Магический артефакт безопасно отключён");
    }
}

public class Solution {
    public static void main(String[] args) {
        // try-with-resources гарантирует автоматический вызов close()
        // даже если внутри блока произойдет исключение
        try (MagicArtifact artifact = new MagicArtifact()) {
            System.out.println("Идёт работа с магическим артефактом");
            // Здесь могла бы быть любая логика работы с артефактом
        }
        // К этому моменту артефакт уже закрыт, сообщение выведено
    }
}
 */
package ru.javarush.java.core.level22.task02;
/*
Просмотр данных студента 🎓

Вы работаете над системой учета студентов в университете. Вам необходимо иметь возможность быстро просматривать основную информацию о каждом студенте.
Создайте класс-запись, который будет хранить имя студента и его возраст.
После этого, в основной программе, создайте запись для вымышленного студента, присвоив ему имя и возраст.
Ваша задача – затем отдельно вывести на экран сначала только имя этого студента, а потом только его возраст, используя предназначенные для этого методы доступа, которые автоматически предоставляются для записей.

Требования:
•	Для хранения информации о студенте должен быть создан record-класс (класс-запись), а не обычный класс.
•	Record-класс должен иметь два компонента: имя студента (тип String) и возраст студента (тип int).
•	В основной программе должен быть создан объект record-класса с заданными именем и возрастом.
•	Имя и возраст должны выводиться на экран отдельно с помощью методов доступа, автоматически сгенерированных для record-класса.
•	Сначала на экран должно быть выведено только имя студента, затем только возраст.
public class Solution {
    public static void main(String[] args) {
        // Создаем запись для вымышленного студента
        Student student = new Student("Иван Петров", 20);

        // Сначала выводим только имя, затем только возраст — через accessor-методы записи
        System.out.println(student.name()); // метод доступа для компонента name
        System.out.println(student.age());  // метод доступа для компонента age
    }
}

 */

public class Solution {
    public static void main(String[] args) {
        // Создаем запись для вымышленного студента
        Student student = new Student("Иван Петров", 20);

        // Сначала выводим только имя, затем только возраст — через accessor-методы записи
        System.out.println(student.name()); // метод доступа для компонента name
        System.out.println(student.age());  // метод доступа для компонента age
    }
}

record Student(String name, int age) {
}

/*
public class Solution {
    public static void main(String[] args) {
        // Создаем запись для вымышленного студента
        Student student = new Student("Иван Петров", 20);

        // Сначала выводим только имя, затем только возраст — через accessor-методы записи
        System.out.println(student.name()); // метод доступа для компонента name
        System.out.println(student.age());  // метод доступа для компонента age
    }
}

// Класс-запись для хранения имени и возраста студента
record Student(String name, int age) {}
 */
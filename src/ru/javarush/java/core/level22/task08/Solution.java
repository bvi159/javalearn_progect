package ru.javarush.java.core.level22.task08;

/*
Отслеживание показаний датчиков 📊

Вы пишете часть системы для обработки потоковых данных с датчиков. Иногда данные поступают в виде набора числовых значений, которые могут изменяться со временем, но сам "контейнер" для этих данных должен быть простой и неизменяемой записью.
Создайте класс-запись, который будет содержать всего один компонент — массив целых чисел.
В основной программе создайте обычный массив из трёх элементов, например, [10, 20, 30], и передайте его в ваш класс-запись.
Затем измените значение первого элемента исходного массива на другое, например, на 99. В завершение, получите первый элемент массива из объекта вашей записи и выведите его на экран. Это покажет, как изменения во внешнем массиве отражаются на содержимом record-объекта, даже если сам record неизменяем.

Требования:
•	Необходимо создать класс-запись (record) с одним компонентом — массивом целых чисел.
•	В основной программе требуется создать массив из трёх элементов и передать его в конструктор record-класса.
•	После создания объекта record нужно изменить значение первого элемента исходного массива.
•	Следует получить первый элемент массива, который хранится внутри объекта record, и вывести его на экран.
•	Результат должен показать, что изменение исходного массива влияет на данные внутри record-объекта, несмотря на его неизменяемость.

// Демонстрация: record неизменяем по структуре, но может хранить ссылку на изменяемый массив.
public class Solution {
    public static void main(String[] args) {
        // Создаем исходный массив показаний датчиков
        int[] readings = {10, 20, 30};

        // Передаем массив в record-объект (сохраняется та же ссылка на массив)
        IntArrayRecord container = new IntArrayRecord(readings);

        // Меняем первый элемент исходного массива
        readings[0] = 99;

        // Берем первый элемент из массива внутри record и выводим его
        System.out.println(container.values()[0]); // ожидаемый вывод: 99
    }
}

 */

// Демонстрация: record неизменяем по структуре, но может хранить ссылку на изменяемый массив.
public class Solution {
    public static void main(String[] args) {
        // Создаем исходный массив показаний датчиков
        int[] readings = {10, 20, 30};

        // Передаем массив в record-объект (сохраняется та же ссылка на массив)
        IntArrayRecord container = new IntArrayRecord(readings);

        // Меняем первый элемент исходного массива
        readings[0] = 99;

        // Берем первый элемент из массива внутри record и выводим его
        System.out.println(container.values()[0]); // ожидаемый вывод: 99
    }
}

record IntArrayRecord(int[] values) {
}

/*
// Демонстрация: record неизменяем по структуре, но может хранить ссылку на изменяемый массив.
public class Solution {
    public static void main(String[] args) {
        // Создаем исходный массив показаний датчиков
        int[] readings = {10, 20, 30};

        // Передаем массив в record-объект (сохраняется та же ссылка на массив)
        IntArrayRecord container = new IntArrayRecord(readings);

        // Меняем первый элемент исходного массива
        readings[0] = 99;

        // Берем первый элемент из массива внутри record и выводим его
        System.out.println(container.values()[0]); // ожидаемый вывод: 99
    }
}

// Простой record с одним компонентом — массивом целых чисел.
record IntArrayRecord(int[] values) { }
 */
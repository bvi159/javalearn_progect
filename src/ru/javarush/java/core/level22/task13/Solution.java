package ru.javarush.java.core.level22.task13;
/*
Показания термометра на метеостанции 🌡️

Вы разрабатываете систему для метеостанции, которая получает данные о температуре в разных форматах. Хотя основное хранение происходит в градусах Цельсия, иногда данные приходят в Фаренгейтах, и их нужно конвертировать.
Создайте класс-запись, который будет содержать одно значение — температуру в градусах Цельсия.
Затем добавьте в этот класс специальный вспомогательный метод, который позволит создать объект температуры, принимая значение в Фаренгейтах и автоматически переводя его в Цельсии по известной формуле.
В основной программе используйте этот метод, чтобы создать объект температуры, соответствующей температуре кипения воды (212°F), и затем выведите полученное значение в Цельсиях на экран.

Требования:
•	Необходимо создать record-класс для хранения температуры в градусах Цельсия.
•	Record-класс должен содержать только одно поле — температуру в градусах Цельсия.
•	В record-классе должен быть реализован статический вспомогательный метод для создания объекта на основе значения в Фаренгейтах.
•	Вспомогательный метод должен корректно переводить температуру из Фаренгейтов в Цельсии по формуле: C = (F - 32) * 5 / 9.
•	В основной программе необходимо создать объект температуры с помощью вспомогательного метода, передав значение 212 (температура кипения воды в Фаренгейтах).
•	В основной программе необходимо вывести на экран значение температуры в градусах Цельсия, полученное из объекта.

public class Solution {
    public static void main(String[] args) {
        // Создаем объект температуры по значению в Фаренгейтах (212°F — кипение воды)
        Temperature boiling = Temperature.fromFahrenheit(212);

        // Выводим значение в градусах Цельсия
        System.out.println(boiling.celsius());
    }
}

 */
public class Solution {
    public static void main(String[] args) {
        // Создаем объект температуры по значению в Фаренгейтах (212°F — кипение воды)
        Temperature boiling = Temperature.fromFahrenheit(212);

        // Выводим значение в градусах Цельсия
        System.out.println(boiling.celsius());
    }
}

record Temperature(double celsius) {
    public static Temperature fromFahrenheit(double farTemp) {
        return new Temperature((farTemp - 32) * 5 / 9);
    }
}

/*

public class Solution {
    public static void main(String[] args) {
        // Создаем объект температуры по значению в Фаренгейтах (212°F — кипение воды)
        Temperature boiling = Temperature.fromFahrenheit(212);

        // Выводим значение в градусах Цельсия
        System.out.println(boiling.celsius());
    }
}

// Record-класс для хранения температуры только в градусах Цельсия
record Temperature(double celsius) {

    // Статический вспомогательный метод (фабричный) для создания Temperature из Фаренгейтов
    public static Temperature fromFahrenheit(double fahrenheit) {
        // Формула перевода: C = (F - 32) * 5 / 9
        double celsius = (fahrenheit - 32) * 5.0 / 9.0; // Используем дробные коэффициенты для точности
        return new Temperature(celsius);
    }
}

 */
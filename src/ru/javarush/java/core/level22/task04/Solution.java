package ru.javarush.java.core.level22.task04;

/*

Управление доступом пользователей в приложении 🔑

Вы создаете систему управления доступом для нового приложения, где каждому пользователю присваивается определенная роль (например, "администратор" или "гость"). Вам нужно быстро находить роль пользователя, используя его личные данные. Создайте класс-запись, который будет представлять пользователя, содержащий его имя и уникальный идентификатор.
Затем организуйте коллекцию, где в качестве ключа будет выступать объект пользователя, а в качестве значения — строка, описывающая его роль. Добавьте в эту коллекцию несколько пользователей с различными идентификаторами и соответствующими ролями. После этого, создайте новый объект пользователя с такими же именем и идентификатором, как у одного из уже добавленных, и попробуйте найти его роль в вашей коллекции. Выведите найденную роль на экран, чтобы проверить, как система распознает существующего пользователя по его данным.

Требования:
•	Должен быть создан record-класс, представляющий пользователя, с двумя компонентами: имя (String) и уникальный идентификатор (например, int или long).
•	Коллекция для хранения ролей пользователей должна использовать объекты record-класса пользователя в качестве ключей.
•	В качестве значения коллекции должна использоваться строка, описывающая роль пользователя (например, "администратор", "гость").
•	В коллекцию должны быть добавлены несколько различных пользователей с уникальными идентификаторами и соответствующими ролями.
•	Должен быть создан новый объект пользователя с теми же именем и идентификатором, что и у одного из уже добавленных пользователей, и осуществлен поиск его роли в коллекции.
•	Роль, найденная по новому объекту пользователя, должна быть выведена на экран для проверки корректности поиска по ключу-объекту record.

import java.util.HashMap;
import java.util.Map;

public class Solution {
    public static void main(String[] args) {
        // Коллекция, где ключ — пользователь (record), значение — строковая роль
        Map<User, String> roles = new HashMap<>();

        // Добавляем нескольких пользователей с уникальными идентификаторами и ролями
        roles.put(new User("Алиса", 1L), "администратор");
        roles.put(new User("Боб", 2L), "модератор");
        roles.put(new User("Ева", 3L), "гость");

        // Создаём новый объект пользователя с теми же данными, что у уже добавленного
        User lookup = new User("Боб", 2L);

        // Ищем его роль: HashMap использует equals/hashCode, которые у record генерируются по компонентам
        String role = roles.get(lookup);

        // Выводим найденную роль на экран
        System.out.println(role);
    }
}

 */

import java.util.HashMap;
import java.util.Map;

public class Solution {
    public static void main(String[] args) {
        // Коллекция, где ключ — пользователь (record), значение — строковая роль
        Map<User, String> roles = new HashMap<>();
        User unknowWho = new User("Боб", 2L);

        // Добавляем нескольких пользователей с уникальными идентификаторами и ролями
//        roles.put(unknowWho, "непоймикто");
        roles.put(new User("Алиса", 1L), "администратор");
        roles.put(new User("Боб", 2L), "модератор");
        roles.put(new User("Ева", 3L), "гость");

        // Создаём новый объект пользователя с теми же данными, что у уже добавленного
        User lookup = new User("Боб", 2L);

        // Ищем его роль: HashMap использует equals/hashCode, которые у record генерируются по компонентам
        String role = roles.get(lookup);

        // Выводим найденную роль на экран
        System.out.println(role);
        System.out.println(roles.get(unknowWho));
        System.out.println(roles.get(lookup).equals(roles.get(unknowWho)));
    }

}

record User(String name, long id) {

}

/*

public class Solution {
    public static void main(String[] args) {
        // Коллекция, где ключ — пользователь (record), значение — строковая роль
        Map<User, String> roles = new HashMap<>();

        // Добавляем нескольких пользователей с уникальными идентификаторами и ролями
        roles.put(new User("Алиса", 1L), "администратор");
        roles.put(new User("Боб", 2L), "модератор");
        roles.put(new User("Ева", 3L), "гость");

        // Создаём новый объект пользователя с теми же данными, что у уже добавленного
        User lookup = new User("Боб", 2L);

        // Ищем его роль: HashMap использует equals/hashCode, которые у record генерируются по компонентам
        String role = roles.get(lookup);

        // Выводим найденную роль на экран
        System.out.println(role);
    }
}

// record-класс пользователя: неизменяемые компоненты name и id
record User(String name, long id) { }

 */
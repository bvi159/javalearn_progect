package ru.javarush.java.core.level22.task18;

/*
Проверка совпадения координат на карте 🗺️

Представьте, что вы работаете над картографическим приложением, где важно точно определять местоположение. Вам нужно удобно представлять координаты и убедиться, что две точки, находящиеся в одном и том же месте, считаются идентичными.
Создайте класс-запись, который будет представлять собой точку на карте с координатами по горизонтали и вертикали.
В основной программе создайте два объекта точек с абсолютно одинаковыми значениями координат. Затем, используя встроенный метод сравнения, проверьте, являются ли эти две точки одинаковыми. Выведите результат сравнения на экран, чтобы подтвердить их идентичность.

Требования:
•	Для представления точки на карте необходимо создать record-класс с двумя полями: координатой по горизонтали и координатой по вертикали.
•	В основной программе должны быть созданы два объекта record-класса с одинаковыми значениями координат.
•	Для проверки идентичности точек должен быть использован встроенный метод сравнения (equals), предоставляемый record-классом.
•	Результат сравнения двух точек должен быть выведен на экран.

public class Solution {
    public static void main(String[] args) {
        // Создаем две точки с одинаковыми координатами
        Point p1 = new Point(10, 20);
        Point p2 = new Point(10, 20);

        // Сравниваем точки с помощью equals, сгенерированного record-классом
        boolean same = p1.equals(p2);

        // Выводим результат сравнения
        System.out.println(same);
    }
}

 */

public class Solution {
    public static void main(String[] args) {
        // Создаем две точки с одинаковыми координатами
        Point p1 = new Point(10, 20);
        Point p2 = new Point(10, 20);

        // Сравниваем точки с помощью equals, сгенерированного record-классом
        boolean same = p1.equals(p2);

        // Выводим результат сравнения
        System.out.println(same);
    }
}

record Point(int horizontal, int vertical) {
}
/*
// Класс-запись (record) для представления точки на карте.
record Point(int horizontal, int vertical) {}
 */
package ru.javarush.java.core.level22.task11;
/*
Красивое представление профиля пользователя 🧑‍💻

Вы создаёте профили пользователей для социальной сети, и вам нужно, чтобы информация о пользователе отображалась более дружелюбно и информативно, чем стандартный вывод.
Создайте класс-запись, который будет хранить имя пользователя и его возраст.
Затем внутри этого класса переопределите стандартный метод, который отвечает за текстовое представление объекта, таким образом, чтобы он возвращал строку в формате "Пользователь: [Имя] ([Возраст] лет)".
В основной программе создайте объект пользователя с любым именем и возрастом, и выведите его на экран.
Ваша задача — убедиться, что вместо стандартного вывода вы получите отформатированную строку, как в примерах профилей.

Требования:
•	В программе должен быть определён record-класс для хранения профиля пользователя, содержащий как минимум два компонента: имя пользователя и возраст.
•	Внутри record-класса должен быть явно переопределён метод toString.
•	Переопределённый метод toString должен возвращать строку строго в формате: "Пользователь: [Имя] ([Возраст] лет)".
•	В основной программе должен быть создан объект пользователя с произвольными именем и возрастом.
•	В основной программе должен быть осуществлён вывод созданного объекта пользователя на экран, чтобы была видна работа переопределённого метода toString.

public class Solution {
    public static void main(String[] args) {
        // Создаем пользователя с произвольными данными
        UserProfile user = new UserProfile("Алиса", 25);

        // Печатаем объект — будет вызван наш переопределенный toString()
        System.out.println(user);
    }
}
 */


public class Solution {
    public static void main(String[] args) {
        // Создаем пользователя с произвольными данными
        UserProfile user = new UserProfile("Алиса", 25);

        // Печатаем объект — будет вызван наш переопределенный toString()
        System.out.println(user);
    }
}

record UserProfile(String name, int age) {
    @Override
    public String toString() {
        return "Пользователь: " + name +
                " (" + age + " лет)";
    }
}

/*
public class Solution {
    public static void main(String[] args) {
        // Создаем пользователя с произвольными данными
        UserProfile user = new UserProfile("Алиса", 25);

        // Печатаем объект — будет вызван наш переопределенный toString()
        System.out.println(user);
    }
}

// record-класс хранит имя и возраст пользователя
record UserProfile(String name, int age) {
    @Override
    public String toString() {
        // Возвращаем строку строго заданного формата
        return "Пользователь: " + name + " (" + age + " лет)";
    }
}
 */
package ru.javarush.java.core.level22.task06;
/*
Данные исторических личностей 🗿

Представьте, что вы каталогизируете данные о великих исторических личностях, и вам важно, чтобы однажды занесённая информация оставалась неизменной.
Создайте класс-запись, который будет хранить имя человека и его возраст (на момент смерти или расцвета).
Затем в основной программе создайте объект для какой-нибудь исторической фигуры, например, "Леонардо да Винчи" и его возраст.
Теперь попробуйте изменить имя этого человека на другое, используя стандартные подходы, которые применяются для обычных классов.

Требования:
•	Необходимо создать record-класс для хранения имени и возраста исторической личности.
•	Record-класс должен содержать два поля: имя (String) и возраст (int).
•	В основной программе необходимо создать объект record-класса с конкретными значениями имени и возраста.
•	В основной программе нужно попытаться изменить имя или возраст объекта, используя обычные подходы (например, присваивание через точку или сеттер).
•	В ходе выполнения задачи должно быть показано, что изменить поля объекта record-класса после его создания невозможно стандартными средствами.

public class Solution {
    public static void main(String[] args) {
        // Создаем объект record-класса с именем и возрастом
        HistoricalFigure figure = new HistoricalFigure("Леонардо да Винчи", 67);

        System.out.println("Исходные данные: " + figure);
        System.out.println("Имя: " + figure.name() + ", возраст: " + figure.age());

        // Попытка 1: прямое присваивание полю (как в обычном классе)
        figure.name = "Микеланджело"; // Ошибка: поля record приватные и final, прямой доступ невозможен.

        // Попытка 2: вызов сеттера (как в обычном классе)
        figure.setName("Микеланджело"); // Ошибка: у record нет сеттеров, такого метода не существует.

        // Объект остается неизменным
        System.out.println("После «попыток изменения» объект не изменился: " + figure);
    }
}

 */
public class Solution {
    public static void main(String[] args) {
        // Создаем объект record-класса с именем и возрастом
        HistoricalFigure figure = new HistoricalFigure("Леонардо да Винчи", 67);

        System.out.println("Исходные данные: " + figure);
        System.out.println("Имя: " + figure.name() + ", возраст: " + figure.age());

        // Попытка 1: прямое присваивание полю (как в обычном классе)
//        figure.name = "Микеланджело"; // Ошибка: поля record приватные и final, прямой доступ невозможен.

        // Попытка 2: вызов сеттера (как в обычном классе)
//        figure.setName("Микеланджело"); // Ошибка: у record нет сеттеров, такого метода не существует.

    System.out.println("После «попыток изменения» объект не изменился: " + figure);
    //throw new RuntimeException(e);

        // Объект остается неизменным

    }
}

record HistoricalFigure(String name, int age) {

}

/*

 */
package ru.javarush.java.core.level22.task14;
/*
Валидация электронного адреса при регистрации 📧

Вы создаёте систему регистрации пользователей, где крайне важно гарантировать, что каждый введённый адрес электронной почты является корректным.
Создайте класс-запись, который будет хранить электронный адрес. Внутри этого класса реализуйте специальный, "компактный" конструктор, который будет автоматически проверять переданную строку: если она пуста, равна null или не содержит символа @, конструктор должен сообщить об ошибке.
В основной программе сначала попробуйте создать корректный электронный адрес и выведите его на экран. Затем, во втором случае, попытайтесь создать заведомо некорректный адрес, например, без символа @, чтобы убедиться, что ваша система валидации успешно поймает ошибку и завершит выполнение программы с соответствующим сообщением.

Требования:
•	Необходимо создать record-класс для хранения электронного адреса.
•	Внутри record-класса должен быть реализован компактный конструктор.
•	Компактный конструктор должен проверять, что переданная строка не пуста, не равна null и содержит символ '@'.
•	Если строка не проходит валидацию, конструктор должен выбрасывать исключение с соответствующим сообщением об ошибке.
•	В основной программе необходимо создать объект record-класса с корректным email и вывести его на экран.
•	В основной программе нужно попытаться создать объект с некорректным email - должно выбрасываться исключение с ожидаемым сообщением.

public class Solution {
    public static void main(String[] args) {
        // 1) Корректный email — создаем и выводим
        Email good = new Email("student@example.com");
        System.out.println("Создан корректный адрес: " + good.address());

        // 2) Некорректный email — без символа '@'
        // Эта строка выбросит IllegalArgumentException с сообщением из конструктора и завершит программу
        Email bad = new Email("student.example.com");

        // До этой строки выполнение не дойдет
        System.out.println(bad.address());
    }
}

 */


public class Solution {
    public static void main(String[] args) {
        // 1) Корректный email — создаем и выводим
        Email good = new Email("student@example.com");
        System.out.println("Создан корректный адрес: " + good.address());

        // 2) Некорректный email — без символа '@'
        // Эта строка выбросит IllegalArgumentException с сообщением из конструктора и завершит программу
        Email bad = new Email("student.example.com");

        // До этой строки выполнение не дойдет
        System.out.println(bad.address());
    }
}

record Email(String address) {
    public Email{
        if (address == null||address==""||address.isEmpty()||!address.contains("@")) {
            throw new IllegalArgumentException("Ошибочка: " + address);
        }
    }
}

/*
// Record-класс для хранения электронного адреса с валидацией в компактном конструкторе
record Email(String address) {
    // Компактный конструктор record-класса: параметр address доступен неявно
    public Email {
        // Простая валидация: null, пустая/пробельная строка и отсутствие символа '@'
        if (address == null || address.isBlank() || !address.contains("@")) {
            throw new IllegalArgumentException(
                "Некорректный email: строка не должна быть null/пустой и обязана содержать символ '@'."
            );
        }
        // Присваивание поля произойдет автоматически после проверки (особенность compact-конструктора)
    }
}

public class Solution {
    public static void main(String[] args) {
        // 1) Корректный email — создаем и выводим
        Email good = new Email("student@example.com");
        System.out.println("Создан корректный адрес: " + good.address());

        // 2) Некорректный email — без символа '@'
        // Эта строка выбросит IllegalArgumentException с сообщением из конструктора и завершит программу
        Email bad = new Email("student.example.com");

        // До этой строки выполнение не дойдет
        System.out.println(bad.address());
    }
}
 */
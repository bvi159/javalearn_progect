package ru.javarush.java.core.level26.task10;
/*
Волшебные универсальные сундуки 📦✨

Представьте, что вы создаёте волшебные сундуки для своей игры. Эти сундуки должны быть универсальными – они могут хранить что угодно: золотые монеты, загадочные свитки или даже мощные артефакты, не требуя для этого отдельных типов сундуков.
Опишите такой универсальный сундук, который сможет принимать и отдавать любой предмет, не зная заранее, что именно в нём будет лежать. У него должно быть приватное поле для содержимого и методы для помещения и извлечения предмета.
Теперь создайте два таких сундука: один для хранения тайного послания (строка), а другой — для магического числа (целое число). Положите в каждый сундук соответствующее "сокровище". И, наконец, достаньте содержимое из обоих сундуков и покажите его на экране, чтобы убедиться, что ваши волшебные контейнеры работают так, как надо!

Требования:
•	Необходимо создать класс сундука с использованием обобщённого типа (generic), чтобы он мог хранить объекты любого типа.
•	В классе сундука должно быть приватное поле, в котором будет храниться предмет типа, определяемого дженериком.
•	В классе сундука должны быть реализованы методы для помещения предмета в сундук и для извлечения предмета из сундука.
•	В основной программе должны быть созданы два экземпляра сундука: один с типом String, другой с типом Integer.
•	В сундук типа String необходимо поместить строку (тайное послание), а в сундук типа Integer — целое число (магическое число).
•	Содержимое обоих сундуков должно быть извлечено с помощью соответствующего метода и выведено на экран.

public class Solution {
    public static void main(String[] args) {
        // Создаем "универсальный сундук" для строк
        UniversalChest<String> messageChest = new UniversalChest<>();
        messageChest.put("Тайное послание: «Да пребудет с тобой Java!»");

        // Создаем "универсальный сундук" для целых чисел
        UniversalChest<Integer> numberChest = new UniversalChest<>();
        numberChest.put(777); // магическое число

        // Извлекаем содержимое сундуков
        String secretMessage = messageChest.take();
        Integer magicNumber = numberChest.take();

        // Выводим содержимое на экран
        System.out.println(secretMessage);
        System.out.println(magicNumber);
    }
}

// Универсальный сундук: обобщенный класс, способный хранить объект любого типа T
class UniversalChest<T> {
    // Приватное поле для хранения "сокровища" типа T


    // Метод для помещения предмета в сундук
    public void put(T item) {

    }

    // Метод для извлечения предмета из сундука
    // Возвращает предмет и "опустошает" сундук, чтобы подчеркнуть идею извлечения
    public T take() {

    }
}

 */
public class Solution {
    public static void main(String[] args) {
        // Создаем "универсальный сундук" для строк
        UniversalChest<String> messageChest = new UniversalChest<>();
        messageChest.put("Тайное послание: «Да пребудет с тобой Java!»");

        // Создаем "универсальный сундук" для целых чисел
        UniversalChest<Integer> numberChest = new UniversalChest<>();
        numberChest.put(777); // магическое число

        // Извлекаем содержимое сундуков
        String secretMessage = messageChest.take();
        Integer magicNumber = numberChest.take();

        // Выводим содержимое на экран
        System.out.println(secretMessage);
        System.out.println(magicNumber);
    }
}

// Универсальный сундук: обобщенный класс, способный хранить объект любого типа T
class UniversalChest<T> {
    // Приватное поле для хранения "сокровища" типа T
    private T field;
//    UniversalChest(T something) {
//        field = something;
//    }

    // Метод для помещения предмета в сундук
    public void put(T item) {
        this.field = item;
    }

    // Метод для извлечения предмета из сундука
    // Возвращает предмет и "опустошает" сундук, чтобы подчеркнуть идею извлечения
    public T take() {
        T item = field;
        field = null;
        return item;
    }
}

/*

// Универсальный сундук: обобщенный класс, способный хранить объект любого типа T
class UniversalChest<T> {
    // Приватное поле для хранения "сокровища" типа T
    private T content;

    // Метод для помещения предмета в сундук
    public void put(T item) {
        this.content = item;
    }

    // Метод для извлечения предмета из сундука
    // Возвращает предмет и "опустошает" сундук, чтобы подчеркнуть идею извлечения
    public T take() {
        T item = content; // сохраняем ссылку на содержимое
        content = null;   // очищаем сундук
        return item;
    }
}

 */
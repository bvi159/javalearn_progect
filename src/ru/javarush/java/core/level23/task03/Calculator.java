package ru.javarush.java.core.level23.task03;
/*
Метод внутри метода: Архитектурный запрет 🏗️

Вы амбициозный разработчик и решили создать собственный калькулятор. Для начала вы определяете главный класс для его логики, назовите его Calculator, сделав его публичным, чтобы он был доступен отовсюду. Внутри этого Calculator вы планируете создать основной метод, который будет выполнять все расчеты – public void calculate().
Теперь, представьте, что вы пытаетесь быть слишком "креативным" и решаете объявить еще один метод, например, public void printResult(), прямо внутри уже существующего метода calculate(). Ваша IDE или компилятор тут же даст вам знать, что так нельзя! Программа не скомпилируется, потому что в Java методы могут существовать только внутри классов, а не внутри других методов, это фундаментальное правило структуры кода.

Требования:
•	В коде должен быть объявлен класс с именем Calculator, помеченный модификатором public.
•	Все методы должны объявляться только на уровне класса Calculator, а не внутри других методов.
•	Внутри метода calculate() не допускается объявление другого метода, например, printResult().
•	В классе Calculator должен присутствовать публичный метод void calculate().
•	Если попытаться объявить метод внутри метода, код не должен компилироваться согласно правилам Java.

public class Calculator {
    // Простой калькулятор: храним последний результат
    private int lastResult;

    // Публичный метод calculate() — точка выполнения вычислений
    public void calculate() {
        int a = 10;
        int b = 5;

        // Пример вычислений
        lastResult = a * b + (a - b);

        // код не скомпилируется, т.к. методы объявляются только внутри класса.


        printResult(); // выносим печать результата в отдельный метод уровня класса
    }

    // Метод объявлен на уровне класса (как и должно быть), а не внутри calculate()


    // Небольшая демонстрация работы
    public static void main(String[] args) {
        new Calculator().calculate();
    }
}

 */
public class Calculator {
    // Простой калькулятор: храним последний результат
    private int lastResult;

    // Публичный метод calculate() — точка выполнения вычислений
    public void calculate() {
        int a = 10;
        int b = 5;

        // Пример вычислений
        lastResult = a * b + (a - b);

        // код не скомпилируется, т.к. методы объявляются только внутри класса.
        printResult();// выносим печать результата в отдельный метод уровня класса




    }

    public void printResult(){
        System.out.println("Печатаю результат" + lastResult);
    }

    // Метод объявлен на уровне класса (как и должно быть), а не внутри calculate()


    // Небольшая демонстрация работы
    public static void main(String[] args) {
        new Calculator().calculate();
    }
}

/*

    public class Calculator {
    // Простой калькулятор: храним последний результат
    private int lastResult;

    // Публичный метод calculate() — точка выполнения вычислений
    public void calculate() {
        int a = 10;
        int b = 5;

        // Пример вычислений
        lastResult = a * b + (a - b);

        // код не скомпилируется, т.к. методы объявляются только внутри класса.
        public void printResult() {
            System.out.println(lastResult);
        }

        printResult(); // выносим печать результата в отдельный метод уровня класса
    }

    // Метод объявлен на уровне класса (как и должно быть), а не внутри calculate()
    public void printResult() {
        System.out.println("Результат вычислений: " + lastResult);
    }

    // Небольшая демонстрация работы
    public static void main(String[] args) {
        new Calculator().calculate();
    }
}
 */

/*

 */
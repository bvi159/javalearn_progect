package ru.javarush.java.core.level23.task02;
/*
Опасность "пустых" книг в библиотечном каталоге 👻

Представьте, что вы библиотекарь, который ведет электронный каталог книг. У вас уже есть где-то определен "чертеж" для книг – класс Book, доступный для всех. Ваша задача — посмотреть, что произойдет, если вы попытаетесь работать с книгой, которую вы объявили в своем каталоге, но которую физически еще не поставили на полку, то есть не создали ее экземпляр.
Объявите переменную, например, libraryBook, которая будет ссылаться на объект типа Book, но намеренно не присваивайте ей никакого реального объекта, оставив ее "пустой". Затем, попробуйте "взять" эту "несуществующую" книгу и попытаться что-либо с ней сделать, например, получить ее название или вызвать любой из ее методов. Вы увидите, как программа "споткнется" и вызовет ошибку во время выполнения – NullPointerException – это будет означать, что вы пытались взаимодействовать с тем, чего просто нет.
Требования:
•	Необходимо объявить переменную типа Book, например, с именем libraryBook.
•	Переменной libraryBook не должно быть присвоено никакого объекта, она должна остаться равной null.
•	В программе должна быть попытка вызвать метод или получить поле у переменной libraryBook.
•	В результате попытки обращения к методу или полю у переменной libraryBook должно возникнуть исключение NullPointerException во время выполнения программы.
•	Класс Book должен быть определен в проекте, чтобы можно было объявить переменную этого типа.

// Простой класс-категория "Книга" с одним полем и методом.
// Он нужен, чтобы тип Book был доступен проекту и компилировался.
public class Book {
    private final String title;

    public Book(String title) {
        this.title = title;
    }

    // Метод, который мы будем пытаться вызвать у null-ссылки
    public String getTitle() {
        return title;
    }
}

 */
// Простой класс-категория "Книга" с одним полем и методом.
// Он нужен, чтобы тип Book был доступен проекту и компилировался.
public class Book {
    private final String title;

    public Book(String title) {
        this.title = title;
    }

    // Метод, который мы будем пытаться вызвать у null-ссылки
    public String getTitle() {
        return title;
    }
}

/*

 */
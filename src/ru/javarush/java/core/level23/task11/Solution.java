package ru.javarush.java.core.level23.task11;

/*
Защита паролей: Никаких лазеек! 🔐

Вы разрабатываете систему безопасности для нового приложения, и самое критичное — это защита пользовательских паролей. Создайте класс User, который будет представлять каждого зарегистрированного пользователя. Внутри этого класса объявите строковое поле userPassword и обязательно сделайте его private. Это означает, что пароль будет доступен только внутри самого класса User, обеспечивая его конфиденциальность.
Теперь, создайте отдельный класс UserService, который, как вы могли бы подумать, должен управлять пользователями. Попробуйте в этом UserService создать объект User и затем, прямо "через забор", попытайтесь напрямую присвоить значение полю userPassword, например, user.userPassword = "12345";. Компилятор сразу же остановит вас! Он выдаст ошибку, потому что private поля – это личное дело класса, и прямой доступ к ним извне строго запрещён, подтверждая, что ваша система безопасности работает как надо.

Требования:
•	В программе должен быть создан класс с именем User.
•	В классе User должно быть объявлено строковое поле userPassword с модификатором доступа private.
•	В программе должен быть создан отдельный класс с именем UserService.
•	В классе UserService необходимо создать объект класса User и попытаться напрямую присвоить значение его полю userPassword (например, user.userPassword = "12345";).
•	Попытка присвоения значения полю userPassword из класса UserService должна привести к ошибке компиляции, так как поле userPassword объявлено с модификатором private.

// Класс, описывающий пользователя
class User {
    // Приватное строковое поле: доступно только внутри класса User
    private String userPassword;
}

// Сервис для работы с пользователями
class UserService {
    public static void main(String[] args) {
        // Создаем объект пользователя


        // Попытка напрямую присвоить значение приватному полю из другого класса:

    }
}

 */

// Класс, описывающий пользователя
class User {
    // Приватное строковое поле: доступно только внутри класса User
    private String userPassword;
}

// Сервис для работы с пользователями
class UserService {
    public static void main(String[] args) {
        // Создаем объект пользователя
        User barsik = new User();

        // Попытка напрямую присвоить значение приватному полю из другого класса:
//        barsik.userPassword = "12345";

    }
}

/*
// Сервис для работы с пользователями
class UserService {
    public static void main(String[] args) {
        // Создаем объект пользователя
        User user = new User();

        // Попытка напрямую присвоить значение приватному полю из другого класса:
        user.userPassword = "12345"; // Ошибка компиляции — доступ к private-полю запрещен
    }
}
 */
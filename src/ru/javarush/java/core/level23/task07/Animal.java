package ru.javarush.java.core.level23.task07;

/*
Строгое требование имени для каждого питомца 🐱


Вы создаете систему учета питомцев для зоопарка, и для вас очень важно, чтобы каждое животное имело имя сразу при рождении или регистрации.
Создайте класс Animal и дайте ему конструктор Animal(String petName), который принимает имя, но не создавайте никакого конструктора без параметров. Это гарантирует, что любое Animal всегда будет иметь имя.
Теперь, создайте класс Cat, который, конечно же, наследует от Animal. Попробуйте добавить в класс Cat свой собственный конструктор без параметров.
Когда вы попытаетесь скомпилировать этот код, компилятор сразу же напомнит вам о вашем "долге"! Он выдаст ошибку, потому что каждый раз, когда вы создаете объект дочернего класса (в данном случае, Cat), сначала должен быть вызван конструктор его родительского класса (Animal). И так как у Animal нет конструктора без параметров, Java не знает, как вызвать его автоматически, и требует от вас явного указания, какой именно конструктор родителя использовать, и какие параметры ему передать.

Требования:
•	Класс Animal должен иметь только конструктор с параметром String, принимающим имя животного.
•	В классе Animal не должно быть конструктора без параметров (по умолчанию).
•	Класс Cat должен наследоваться от класса Animal с помощью ключевого слова extends.
•	Класс Cat должен содержать собственный конструктор без параметров.
•	Если в конструкторе Cat не вызывается явно конструктор родительского класса Animal с параметром, компилятор выдаёт ошибку.

// Класс Animal отвечает за любое животное и гарантирует, что у него всегда есть имя
public class Animal {
    // Поле для хранения имени животного
    private final String name;

    // Конструктор, принимающий имя животного
    // В классе нет конструктора без параметров, чтобы нельзя было создать животное без имени
    public Animal(String petName) {
        this.name = petName;
    }

    // Геттер для получения имени животного
    public String getName() {
        return name;
    }
}

 */

// Класс Animal отвечает за любое животное и гарантирует, что у него всегда есть имя
public class Animal {
    public static void main(String[] args) {
        Cat barsik = new Cat();
        System.out.println(barsik.getName());
    }

    // Поле для хранения имени животного
    private final String name;

    // Конструктор, принимающий имя животного
    // В классе нет конструктора без параметров, чтобы нельзя было создать животное без имени
    public Animal(String petName) {
        this.name = petName;
    }

    // Геттер для получения имени животного
    public String getName() {
        return name;
    }
}

class Cat extends Animal {
    //    21.07.2026 - закоментировано правильное решение, приводящее к ошибке
    Cat() {
       super("Барсик");
    }
}
/*
// Класс Cat наследуется от Animal
public class Cat extends Animal {
    // Конструктор без параметров
    public Cat() {
        // Передаём родительскому классу Animal некоторое имя по умолчанию

    }
}
 */
package ru.javarush.java.core.level23.task18;

/*
Ясные имена для ясности системы 💬

Вы унаследовали часть кода от предыдущего разработчика, и он выглядит немного загадочно: int a = 10; String s = "Привет"; void f() { System.out.println(s + " " + a); }
Его функциональность понятна – он выводит сообщение с числом. Но имена a, s, f совершенно не говорят о том, что они делают.
Ваша миссия — дать этим переменным и методу осмысленные имена, которые сразу объясняют их назначение, следуя принятым в Java соглашениям об именовании (camelCase для переменных и методов).
Создайте класс и внутри него переименуйте целочисленную переменную так, чтобы она отражала “количество” или “значение”, а строковую переменную — чтобы она представляла “сообщение” или “текст”. Переименуйте метод так, чтобы его название явно указывало на действие “отобразить” или “вывести сообщение”.
После переименования убедитесь, что ваш обновленный метод все еще выводит на экран желаемую строку "Привет 10".

Требования:
•	Переменная типа int должна быть переименована так, чтобы ее имя отражало "количество" или "значение" (например, count, value, number).
•	Переменная типа String должна быть переименована так, чтобы ее имя отражало "сообщение" или "текст" (например, message, text, greeting).
•	Метод должен быть переименован так, чтобы его имя явно указывало на действие "отобразить" или "вывести сообщение" (например, displayMessage, printMessage, showMessage).
•	Имена переменных и метода должны быть оформлены в стиле camelCase согласно Java code conventions.
•	Весь код должен быть размещён внутри отдельного класса.
•	Переименованный метод должен по-прежнему выводить на экран строку "Привет 10".

public class Solution {
    // Осмысленные имена полей


    // Переименованный метод: чётко указывает, что выводит сообщение


    public static void main(String[] args) {
        // Создаем объект и вызываем метод для демонстрации результата
        Solution app = new Solution();
        app.printMessage();
    }
}

 */

public class Solution {
    // Осмысленные имена полей
    int quantity = 10;
    String message = "Привет";

    void printMessage() {
        System.out.println(message + " " + quantity);
    }

    // Переименованный метод: чётко указывает, что выводит сообщение


    public static void main(String[] args) {
        // Создаем объект и вызываем метод для демонстрации результата
        Solution app = new Solution();
        app.printMessage();
    }
}

/*

public class Solution {
    // Осмысленные имена полей
    private int value = 10;
    private String message = "Привет";

    // Переименованный метод: чётко указывает, что выводит сообщение
    void printMessage() {
        System.out.println(message + " " + value); // Ожидаемый вывод: "Привет 10"
    }

    public static void main(String[] args) {
        // Создаем объект и вызываем метод для демонстрации результата
        Solution app = new Solution();
        app.printMessage();
    }
}

 */
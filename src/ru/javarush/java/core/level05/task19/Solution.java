package ru.javarush.java.core.level05.task19;
/*
Создание цифровой шахматной доски ♟

Вы — художник цифровых миров, и сегодня ваше задание — нарисовать классическое поле битвы — шахматную доску.
Каждая клетка должна быть либо светлой ##, либо тёмной __, и они должны чередоваться идеально,
 создавая узнаваемый узор. Ваша доска должна быть стандартного размера 8x8,
 и начинать её нужно со светлой клетки в левом верхнем углу.

Напишите программу, которая с помощью чередующихся символов ## и __ изобразит идеальную доску размером 8x8. У
бедитесь, что первая строка начинается с ##, и затем символы корректно чередуются как по горизонтали,
так и по вертикали.

Результат работы вашей программы должен выглядеть в точности так:

##__##__##__##__
__##__##__##__##
##__##__##__##__
__##__##__##__##
##__##__##__##__
__##__##__##__##
##__##__##__##__
__##__##__##__##
Требования:
•	Программа должна использовать вложенные циклы для генерации каждой строки и каждой клетки шахматной доски.
•	Программа должна выводить ровно 8 строк по 16 символов в каждой строке (8 клеток по 2 символа).
•	Каждая клетка должна быть либо светлой (##), либо тёмной (__), при этом цвета должны чередоваться как по горизонтали, так и по вертикали.
•	В левом верхнем углу доски (первая клетка первой строки) должен быть символ светлой клетки (##).
•	Программа должна выводить доску в точности как в примере, без дополнительных пробелов, пустых строк или других символов.

public class Solution {
    public static void main(String[] args) {
        int size = 8; // Размер доски 8x8

        // Внешний цикл — по строкам

            // Внутренний цикл — по клеткам строки

                // Чередуем клетки по сумме индексов: чётная — "##", нечётная — "__"


            // Перенос строки после каждой строки доски (без лишних пробелов)

    }
}

 */
public class Solution {
    public static void main(String[] args) {
        int size = 8; // Размер доски 8x8

        // Внешний цикл — по строкам
        for (int i = 0; i < size; i++) {
            // Внутренний цикл — по клеткам строки
            for (int j = 0; j < 8; j++) {
                // Чередуем клетки по сумме индексов: чётная — "##", нечётная — "__"
                if ((j + i) % 2 == 0) {
                    System.out.print("##");
                }else {
                    System.out.print("__");
                }
            }
            // Перенос строки после каждой строки доски (без лишних пробелов)
            System.out.println();
        }
    }
}

/*
Ихнее решение - используют тренарный оператор
public class Solution {
    public static void main(String[] args) {
        int size = 8; // Размер доски 8x8

        // Внешний цикл — по строкам
        for (int row = 0; row < size; row++) {
            // Внутренний цикл — по клеткам строки
            for (int col = 0; col < size; col++) {
                // Чередуем клетки по сумме индексов: чётная — "##", нечётная — "__"
                System.out.print(((row + col) % 2 == 0) ? "##" : "__");
            }
            // Перенос строки после каждой строки доски (без лишних пробелов)
            System.out.println();
        }
    }
}
 */
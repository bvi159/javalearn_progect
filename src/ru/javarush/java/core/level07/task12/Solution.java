package ru.javarush.java.core.level07.task12;

/*
Отображение данных с сенсорной сетки 📊

Вы отслеживаете данные с сетки экологических датчиков, расположенных в 2 рядах и 5 столбцах. Каждый датчик отправляет определенное показание, и для тестирования вы предварительно заполнили их значениями от 10 до 19, двигаясь последовательно по каждой строке.

Теперь ваша цель — представить эти данные ученому в виде четкой и легко читаемой таблицы, где каждая строка показаний датчиков отображается на отдельной строке, а отдельные показания разделены пробелами.

Создайте двумерный массив int[][] sensorData размером 2 строки и 5 столбцов. Заполните его числами от 10 до 19 по порядку (сначала первая строка, затем вторая). Затем выведите весь массив на экран в виде таблицы: каждая строка массива должна быть выведена на отдельной строке, а элементы внутри строки разделены пробелом.

Требования:
•	Необходимо создать двумерный массив int[][] sensorData размером 2 строки и 5 столбцов.
•	Массив sensorData должен быть заполнен числами от 10 до 19 включительно, по строкам слева направо (сначала первая строка, затем вторая).
•	Каждая строка массива sensorData должна быть выведена на отдельной строке экрана.
•	Элементы внутри каждой строки должны быть разделены пробелом при выводе.
•	В выводе между числами не должно быть лишних символов, кроме одного пробела; после последнего числа в строке не должно быть пробела.

public class Solution {
    public static void main(String[] args) {
        // Создаем двумерный массив для хранения показаний (2 строки, 5 столбцов)

        // Начальное значение для заполнения массива


        // Заполняем массив по строкам, слева направо


        // Выводим массив в виде таблицы


        // Печатаем значение элемента

        // Если это не последний элемент в строке, добавляем пробел



        // После каждой строки переходим на новую


    }
}

 */

public class Solution {
    public static void main(String[] args) {
        // Создаем двумерный массив для хранения показаний (2 строки, 5 столбцов)
        int[][] sensorData = new int[2][5];
        // Начальное значение для заполнения массива
        int data = 10;


        // Заполняем массив по строкам, слева направо
        for (int row = 0; row < sensorData.length; row++) {
            // Проходим по столбцам
            for (int col = 0; col < sensorData[row].length; col++) {
                // Заполняем текущую ячейку
                sensorData[row][col] = data;
                // Увеличиваем число для следующей ячейки
                System.out.print(data);
                if (col < sensorData[row].length - 1)
                    System.out.print(" ");
                data++;
            }
            System.out.println();
        }

        // Выводим массив в виде таблицы
        // Печатаем значение элемента


        // Если это не последний элемент в строке, добавляем пробел



        // После каждой строки переходим на новую


    }
}
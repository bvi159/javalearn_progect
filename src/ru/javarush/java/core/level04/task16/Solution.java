package ru.javarush.java.core.level04.task16;
/*
Суммирование ежедневных продаж 💰
Вы фиксируете продажи за день. Вводите суммы каждой продажи с клавиатуры до тех пор, пока не введёте отрицательное число (что означает конец рабочего дня).

Как только вы вводите отрицательное число, программа должна показать итоговую сумму всех продаж и завершиться. Отрицательное число в общую сумму не включается.

Требования:
•	Программа должна использовать цикл для ввода сумм продаж с клавиатуры до тех пор, пока не будет введено отрицательное число.
•	Если пользователь вводит отрицательное число, цикл должен завершиться, и это число не должно учитываться в итоговой сумме.
•	В итоговую сумму должны включаться только положительные значения продаж, введённые до появления отрицательного числа.
•	После завершения ввода программа должна вывести на экран сумму всех введённых положительных чисел.
•	Программа должна корректно считывать и обрабатывать значения, введённые пользователем с клавиатуры.

public class Solution {
    public static void main(String[] args) {
        // Создаем объект Scanner для считывания ввода с клавиатуры


        // Переменная для хранения суммы всех продаж


        // Бесконечный цикл для ввода данных
        while (true) {
            // Запрашиваем сумму продажи

            // Считываем введенное число


            // Если введено отрицательное число, выходим из цикла
            if (/ условие /) {
        break;
        }

        // Добавляем положительное число к общей сумме

        }

        // Выводим итоговую сумму всех продаж

        }
        }
Ихнее решение, хотя наше тоже правильное
public class Solution {
    public static void main(String[] args) {
        // Создаем объект Scanner для считывания ввода с клавиатуры
        Scanner console = new Scanner(System.in);

        // Переменная для хранения суммы всех продаж
        int totalSales = 0;

        // Бесконечный цикл для ввода данных
        while (true) {
            System.out.println("Введите сумму продажи (или отрицательное число для завершения):");
            int sale = console.nextInt(); // Считываем введенное число

            // Если введено отрицательное число, выходим из цикла
            if (sale < 0) {
                break;
            }

            // Добавляем положительное число к общей сумме
            totalSales += sale;
        }

        // Выводим итоговую сумму всех продаж
        System.out.println("Итоговая сумма всех продаж: " + totalSales);
    }
}


 */

import java.util.Scanner;

public class Solution {
    public static void main(String[] args) {
        // Создаем объект Scanner для считывания ввода с клавиатуры
        Scanner scanner = new Scanner(System.in);

        // Переменная для хранения суммы всех продаж
        int summSale = 0;
        int currentSale = 0;

        // Бесконечный цикл для ввода данных
        while (true) {
            // Запрашиваем сумму продажи
            System.out.println("Сумма продажи:");
            // Считываем введенное число
            currentSale = scanner.nextInt();

            // Если введено отрицательное число, выходим из цикла
            if (currentSale < 0) {
                break;
            }

            // Добавляем положительное число к общей сумме
            summSale += currentSale;

        }

        // Выводим итоговую сумму всех продаж
        System.out.println(summSale);

    }
}